# Shared Context — stab-closure-consolidation

**Lifecycle status:** `closed`
**Actualizado:** 2026-08-16 (cierre del incremento)
**Cerrado:** 2026-08-16
**Spec Validator:** `ready` (trazabilidad histórica conservada)

## Current status

`closed` (2026-08-16): incremento exclusivamente documental de cierre y consolidación del incremento `merkee-shop-foundation-stabilization`. Las siete acciones documentales SCC-A1–A7 están **implementadas y verificadas en disco** mediante el task board `docs/specs/tasks/stab-closure-consolidation-task-board.md` (estado superior `done`; las siete tareas `done` con `verification_result: PASS`). El `## Spec Validator Approval` (`verdict: ready`, 2026-08-16) y la `## Human Plan Approval: approved_by_user` (2026-08-16) se conservan como trazabilidad histórica; no se invalidan. No se modificó código, OpenAPI, Prisma, `package.json`, runtime config ni se ejecutó Git.

Evidencia verificada en disco (2026-08-16) de la ejecución de SCC-A1–A7: (SCC-A1) delta spec `merkee-shop-foundation-stabilization-delta-spec.md` línea 3 `## Status: closed` y línea 4 `**Lifecycle status:** closed`; shared context `merkee-shop-foundation-stabilization-sdd-context.md` línea 3 `**Lifecycle status:** closed` y línea 7 `Current status: closed` — aprobaciones históricas `verdict: ready`/`## Human Plan Approval`/`## Controlled Pre-Ready Remediation` conservadas. (SCC-A2) Master Spec §ROP (línea 44) consolida B1 (activate-admin ROP) y B5 (jwt.verify ROP) como regla durable, con TD-NEW-ROP-SIGN referenciada como deuda activa; §Identidad (línea 83) consolida B3 (fail-fast del secreto JWT en producción) con STAB-DEC-12 (`iss`/`aud`/`typ`/proveedor) explícitamente pendiente; `architecture-decisions.md` no reeditado. (SCC-A3) los tres artefactos de estabilización permanecen in-place en `docs/specs/increments/`, `docs/specs/.working/` y `docs/specs/tasks/`; glob `docs/specs/archive/**` sin resultados (no se creó `archive/` ni se movieron archivos). (SCC-A4) TD-NEW-ROP-SIGN, TD-NEW-HTTP-SEC y TD-NEW-COV registradas como `active` en `docs/specs/technical_debt.md` (global, líneas 21-23) y `projects/merkee-shop-api/docs/specs/technical_debt.md` (espejo local, líneas 21-23), contenido idéntico, ninguna `resolved`. (SCC-A5) TD-NEW-STAB-LIFECYCLE registrada en "Cambios completados — no son deuda activa" como `resolved-by-verified-implementation` con evidencia en ambos registros (global línea 37, local línea 37); única deuda nueva marcada resuelta. (SCC-A6) `docs/specs/workspace_changes.md` (líneas 7-15) registra el cierre/consolidación, las deudas TD-NEW-* y la permanencia de los gates legal/AWS; sin rutas/endpoints/schemas/dependencias cross-service nuevos. (SCC-A7) TD-MSF-ID-002-02 (legal/contable) y TD-MSF-ID-002-03 (AWS) permanecen `active` como gates antes de producción en ambos registros (global líneas 12-13, local líneas 12-13) y en `workspace_changes.md`; no se declaran resueltos ni se añade evidencia de cierre.

**Deudas que permanecen `active` (no resueltas en este incremento):** TD-NEW-ROP-SIGN (`try/catch` técnico en `register`/`login`/`refresh-session` use cases), TD-NEW-HTTP-SEC (headers de seguridad HTTP/CORS/CSRF/rate-limit ausentes en `main.ts`) y TD-NEW-COV (`coverageThreshold` ausente en `package.json`). Los gates de producción TD-MSF-ID-002-02 (legal/contable) y TD-MSF-ID-002-03 (AWS) permanecen abiertos. La decisión JWT (`iss`/`aud`/`typ`/proveedor, STAB-DEC-12) sigue pendiente para una delta futura antes de producción. La consolidación final de los registros global/local de deuda se realizará después de terminar los incrementos de deuda técnica (TD-NEW-ROP-SIGN/HTTP-SEC/COV), conforme a la aprobación humana registrada.

**Open questions resueltas (aprobación humana 2026-08-16):** las cuatro open questions del incremento están resueltas y registradas como decisiones locked — OQ-SCC-01 → SCC-DEC-01 confirmada (estado final `closed` para el delta spec y el shared context de estabilización); OQ-SCC-02 → SCC-DEC-06 (mantener artefactos in-place marcados `closed`/históricos, sin crear `docs/specs/archive/`); OQ-SCC-03 → SCC-DEC-07 (consolidar B1-B5 solo en Master Spec, sin reeditar ADR-017); OQ-SCC-04 → SCC-DEC-08 (Task Decomposer crea task board de SCC-A1–A7 tras `verdict: ready` + aprobación humana). El incremento transiciona de `validated-not-executed` a `closed` tras la ejecución y verificación en disco de SCC-A1–A7 mediante el task board creado por Task Decomposer.

## Canonical artifacts

- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/increments/stab-closure-consolidation-delta-spec.md` (esta delta, `closed`)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/increments/merkee-shop-foundation-stabilization-delta-spec.md` (predecesor cerrado por SCC-A1; `closed` in-place)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/.working/merkee-shop-foundation-stabilization-sdd-context.md` (shared context predecesor cerrado por SCC-A1; `closed` in-place)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/tasks/merkee-shop-foundation-stabilization-task-board.md` (`done`; sin cambio por SCC-A3)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/master_spec.md` (B1/B3/B5 consolidados por SCC-A2 en §ROP/§Identidad)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/architecture-decisions.md` (ADR-017 ROP; sin reedición por SCC-DEC-07; la consolidación de B1-B5 ocurre solo en Master Spec)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/technical_debt.md` (TD-NEW-ROP-SIGN/HTTP-SEC/COV registradas `active` por SCC-A4; TD-NEW-STAB-LIFECYCLE `resolved-by-verified-implementation` por SCC-A5; gates confirmados por SCC-A7)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/projects/merkee-shop-api/docs/specs/technical_debt.md` (espejo local; TD-NEW-* registradas idénticas al global por SCC-A4/A5; gates confirmados por SCC-A7)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/workspace_changes.md` (actualizado por SCC-A6 con sección de cierre/consolidación)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/.working/merkee-shop-foundation-sdd-context.md` (shared context del incremento padre `merkee-shop-foundation`; `revision-needed`; referencia, no se modifica en este incremento)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/tasks/merkee-shop-foundation-task-board.md` (task board del incremento padre; `blocked`; referencia, no se modifica en este incremento)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/api/openapi.yaml` (canónico; **solo lectura** para este incremento; no se modifica)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/prisma-migration-contract.md` (canónico; **solo lectura** para este incremento; no se modifica)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/graphify-out/GRAPH_REPORT.md` (evidencia de grafo en disco; solo lectura; frescura respecto a HEAD no verificable sin Git — **blocked para frescura**; cifras 1,245 nodos / 2,603 aristas verificadas en disco)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/projects/merkee-shop-api/src/main.ts` (evidencia TD-NEW-HTTP-SEC; solo lectura; no se modifica)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/projects/merkee-shop-api/package.json` (evidencia TD-NEW-COV; solo lectura; no se modifica)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/projects/merkee-shop-api/src/modules/identity/application/use-cases/{register,login,refresh-session}.use-case.ts` (evidencia TD-NEW-ROP-SIGN; solo lectura; no se modifica)

## Artifact evidence

| Ruta | Campo/flujo verificado | Resultado | Estado |
|---|---|---|---|
| `docs/specs/tasks/merkee-shop-foundation-stabilization-task-board.md` línea 5 | Estado superior del task board de estabilización | `Estado: done` — las cinco tareas STAB-B1/B2/B3/B4/B5 están `status: done` con `verification_result` PASS. | pass |
| `docs/specs/.working/merkee-shop-foundation-stabilization-sdd-context.md` línea 7 | `Current status` del incremento de estabilización | `Current status: implemented. B1-B5 (...) están implementadas y verificadas en disco (2026-08-16).` | pass |
| `docs/specs/.working/merkee-shop-foundation-stabilization-sdd-context.md` línea 3 | `Lifecycle status` del shared context de estabilización | `closed` (corregido por SCC-A1, verificado en disco 2026-08-16); aprobaciones históricas conservadas. | pass (corregido por SCC-A1) |
| `docs/specs/increments/merkee-shop-foundation-stabilization-delta-spec.md` líneas 3-4 | `## Status` y `**Lifecycle status:**` del delta spec de estabilización | `closed` (corregido por SCC-A1, verificado en disco 2026-08-16); `**Spec Validator:** ready` y `**Shared context:**` conservados como trazabilidad. | pass (corregido por SCC-A1) |
| `docs/specs/.working/merkee-shop-foundation-stabilization-sdd-context.md` líneas 56-61 | `## Spec Validator Approval` del incremento de estabilización | `verdict: ready`, `reviewed_at: 2026-08-16T00:00:00-05:00`, `validator_agent: spec-validator`, `invalidated_by_changes_since: none` — se conserva como trazabilidad histórica del incremento cerrado. | pass (trazabilidad histórica) |
| `docs/specs/.working/merkee-shop-foundation-stabilization-sdd-context.md` líneas 63-65 | `## Human Plan Approval: approved_by_user` del incremento de estabilización | Aprobación humana 2026-08-16, alcance B1-B5 — se conserva como trazabilidad histórica del incremento cerrado. | pass (trazabilidad histórica) |
| `docs/specs/technical_debt.md` líneas 30-33 | TD-MSF-STAB-001/002/003/004 | `resolved-by-verified-implementation` (verificadas en disco 2026-08-16): STAB-B3 (JWT fail-fast), STAB-B1 (ROP activate-admin), STAB-B2 (`must_change_password` replay `true`), STAB-B5 (ROP JWT verify). | pass |
| `projects/merkee-shop-api/docs/specs/technical_debt.md` líneas 30-33 | TD-MSF-STAB-001/002/003/004 (espejo local) | `resolved-by-verified-implementation` — idéntico al global. | pass |
| `docs/specs/technical_debt.md` líneas 11-13 | TD-MSF-ID-002-01/02/03 | `active` — scopes desconocidos `operation_pending` (TD-01), revisión legal/contable gate antes de producción (TD-02), AWS no configurado gate antes de producción (TD-03). Permanecen `active` por SCC-A7. | pass |
| `projects/merkee-shop-api/src/modules/identity/application/use-cases/register.use-case.ts` líneas 48-108 | `try { ... } catch { return fail(technicalFailure()); }` envolviendo `jwt.sign` (línea 85) y otros adapters | `try/catch` técnico en `application` envolviendo toda la lógica — contradice Master Spec §ROP. Evidencia TD-NEW-ROP-SIGN. | fail (deuda TD-NEW-ROP-SIGN, active) |
| `projects/merkee-shop-api/src/modules/identity/application/use-cases/login.use-case.ts` líneas 55-129 | `try { ... } catch { return fail(technicalFailure()); }` envolviendo `jwt.sign` (línea 106) y otros adapters | `try/catch` técnico en `application` — contradice Master Spec §ROP. Evidencia TD-NEW-ROP-SIGN. | fail (deuda TD-NEW-ROP-SIGN, active) |
| `projects/merkee-shop-api/src/modules/identity/application/use-cases/refresh-session.use-case.ts` líneas 48-110 | `try { ... } catch { return fail(technicalFailure()); }` envolviendo `jwt.sign` (línea 83) y otros adapters | `try/catch` técnico en `application` — contradice Master Spec §ROP. Evidencia TD-NEW-ROP-SIGN. | fail (deuda TD-NEW-ROP-SIGN, active) |
| `projects/merkee-shop-api/src/main.ts` líneas 1-10 | Configuración de seguridad HTTP | `main.ts` mínimo (10 líneas): `NestFactory.create(AppModule)` + `app.listen(port)`; sin helmet, CSP, HSTS, nosniff, CORS allowlist, CSRF ni rate limiting. Evidencia TD-NEW-HTTP-SEC. | fail (deuda TD-NEW-HTTP-SEC, active) |
| `projects/merkee-shop-api/package.json` líneas 55-71 | Sección `jest` — `coverageThreshold` | Sección `jest` define `moduleFileExtensions`, `rootDir`, `testRegex`, `transform`, `collectCoverageFrom`, `coverageDirectory`, `testEnvironment`; **no define `coverageThreshold`**. `test:cov` genera reporte pero no falla por umbral. Evidencia TD-NEW-COV. | fail (deuda TD-NEW-COV, active) |
| `docs/specs/master_spec.md` AC-05 (línea 21) | `must_change_password: true` en replay (B2) | AC-05 ya ampliado por STAB-A5: "el replay de provisión devuelve `must_change_password: true` (constante de provisión...) consistente con OpenAPI `AdminUserProvisionResponse.must_change_password: { const: true }` (STAB-DEC-01 Opción A)". Ya consolidado; no requiere reedición por SCC-A2. | pass |
| `docs/specs/master_spec.md` §ROP (líneas 61-64) | 404 de provision (B4/STAB-DEC-10) y `DUPLICATE_WEBHOOK_EVENT` clasificación interna (STAB-DEC-11) | §ROP ya aclarado por STAB-DEC-10/11: `provisionAdminUser` declara `404 RESOURCE_NOT_FOUND` en replay; `DUPLICATE_WEBHOOK_EVENT` (409) es clasificación interna no proyectada al proveedor. Ya consolidado; no requiere reedición por SCC-A2. | pass |
| `docs/specs/master_spec.md` §NC-08 (líneas 95-97) | Scheduler driving adapter local (STAB-A6/STAB-DEC-06) | §NC-08 ya aclarado: scheduler materializa `0 0 2 * * *` con driving adapter local (`OnApplicationBootstrap`, `setTimeout` + `HH:MM` UTC, configurable), no cron literal. Ya consolidado; no requiere reedición por SCC-A2. | pass |
| `docs/specs/master_spec.md` §Identidad (líneas 79-83) | JWT fail-fast del secreto (B3) y decisión pendiente `iss`/`aud`/`typ`/proveedor (STAB-DEC-12) | §Identidad consolida B3 (fail-fast del secreto JWT en producción) por SCC-A2 y documenta la decisión pendiente JWT (`iss`/`aud`/`typ`/proveedor no fijados canónicamente; STAB-DEC-12 pendiente; implementación `jsonwebtoken`/HS256 provisional). | pass (B3 consolidado por SCC-A2) |
| `docs/specs/master_spec.md` §ROP (líneas 36-42) | Patrón ROP aplicado a `activate-admin` (B1) y `jwt.verify` (B5) | §ROP consolida B1 (activate-admin ROP) y B5 (jwt.verify ROP) por SCC-A2 como regla durable (puerto `Result`, adapter traduce, application/controller sin `try/catch` técnico); TD-NEW-ROP-SIGN referenciada como deuda activa para register/login/refresh-session. | pass (B1/B5 consolidados por SCC-A2) |
| `graphify-out/GRAPH_REPORT.md` | Grafo de dependencias | 1,245 nodos / 2,603 aristas, 87 comunidades, sin ciclos de importación, 269 nodos aislados; construido desde commit `89bcd155`. Frescura respecto a `HEAD` no verificable sin Git — **blocked para frescura**. | pass (contenido) / blocked (frescura) |

## Spec Validator Approval

verdict: ready
reviewed_at: 2026-08-16T00:00:00-05:00
validator_agent: spec-validator
artifact_set_reviewed: /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/increments/stab-closure-consolidation-delta-spec.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/master_spec.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/increments/merkee-shop-foundation-stabilization-delta-spec.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/.working/merkee-shop-foundation-stabilization-sdd-context.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/tasks/merkee-shop-foundation-stabilization-task-board.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/technical_debt.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/projects/merkee-shop-api/docs/specs/technical_debt.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/workspace_changes.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/architecture-decisions.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/prisma-migration-contract.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/api/openapi.yaml; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/graphify-out/GRAPH_REPORT.md

## Human Plan Approval: approved_by_user

- approved_at: 2026-08-16
- approved_by: user
- alcance: SCC-A1–A7 (acciones documentales exclusivamente; cierre y consolidación del incremento `merkee-shop-foundation-stabilization`). No incluye creación de endpoints, migraciones, schemas OpenAPI, parámetros ni reglas de negocio.
- instrucción de consolidación de deuda: la consolidación final de todos los registros global/local de deuda (`docs/specs/technical_debt.md` y `projects/merkee-shop-api/docs/specs/technical_debt.md`) se realizará **después de terminar los incrementos de deuda técnica** (TD-NEW-ROP-SIGN, TD-NEW-HTTP-SEC, TD-NEW-COV). Durante este incremento `stab-closure-consolidation` **solo se registran las deudas activas** (TD-NEW-ROP-SIGN/HTTP-SEC/COV como `active`) y **se marca resuelta únicamente la deuda de lifecycle** TD-NEW-STAB-LIFECYCLE como `resolved-by-verified-implementation` con evidencia (SCC-A1 + task board `done` + B1-B5 verificados + TD-MSF-STAB-001/002/003/004 resueltas).
- restricciones: no se modifica código (`projects/merkee-shop-api/src/**`), Prisma (`projects/merkee-shop-api/prisma/**`), OpenAPI (`docs/api/openapi.yaml`), `package.json`, `.env*` ni runtime config; no se ejecutan operaciones de Git (commits, branches, PRs).
- gate de ejecución: las acciones documentales SCC-A1–A7 no se ejecutan hasta que Task Decomposer cree el task board correspondiente con estado superior `todo`.

## Decisions locked

- SCC-DEC-01: el incremento `merkee-shop-foundation-stabilization` transiciona a `closed` (estado final: implementado y cerrado/consolidado). Delta spec y shared context de estabilización → `closed`; task board permanece `done`. No se marcan `superseded` (no fueron reemplazados). Las aprobaciones históricas `verdict: ready` y `## Human Plan Approval: approved_by_user` del incremento de estabilización se conservan como trazabilidad histórica; no se invalidan. **OQ-SCC-01 resuelta (aprobación humana 2026-08-16):** el usuario confirmó `closed` como estado final para ambos artefactos.
- SCC-DEC-02: las decisiones durables B1-B5 se consolidan en la Master Spec integrándolas en secciones existentes (§ROP, §Identidad) de forma concisa, sin duplicar el historial STAB-* del incremento de estabilización. Lo ya consolidado (AC-05, §ROP 404/DUPLICATE_WEBHOOK_EVENT, §NC-08 scheduler) no se reedita. Falta consolidar: B1 (activate-admin ROP), B3 (JWT fail-fast del secreto), B5 (jwt.verify ROP).
- SCC-DEC-03: TD-NEW-ROP-SIGN, TD-NEW-HTTP-SEC y TD-NEW-COV se registran como `active` (no resueltas); TD-NEW-STAB-LIFECYCLE se registra como `resolved-by-verified-implementation` con evidencia (la alineación del lifecycle por SCC-A1).
- SCC-DEC-04: la decisión JWT (`iss`/`aud`/`typ` y proveedor) no se fija en este incremento; sigue pendiente (STAB-DEC-12) para una delta futura antes de producción. La consolidación de B3/B5 no cubre `iss`/`aud`/`typ` ni el proveedor JWT.
- SCC-DEC-05: Graphify se referencia como artefacto canónico con contenido verificado en disco (1,245 nodos / 2,603 aristas, sin ciclos), pero la frescura respecto a `HEAD` permanece **blocked para frescura** (no se ejecuta Git ni `graphify --update`). Se recomienda ejecutar `graphify update .` + `git rev-parse HEAD` antes de la revalidación de Spec Validator como verificación defensiva.
- SCC-DEC-06 (OQ-SCC-02 resuelta, aprobación humana 2026-08-16): los artefactos del incremento de estabilización (delta spec, shared context, task board) se mantienen in-place en sus rutas actuales (`docs/specs/increments/`, `docs/specs/.working/`, `docs/specs/tasks/`) marcados `closed`/históricos. No se crea la carpeta `docs/specs/archive/` ni se mueven artefactos a ella. El estado aplicado es `closed` (no `superseded`, porque el incremento no fue reemplazado por una decisión distinta — consistente con SCC-DEC-01).
- SCC-DEC-07 (OQ-SCC-03 resuelta, aprobación humana 2026-08-16): la consolidación durable de B1-B5 ocurre únicamente en `docs/specs/master_spec.md` (§ROP/§Identidad). No se reedita ADR-017 en `docs/specs/architecture-decisions.md`; las decisiones durables B1-B5 son aplicación de ADR-017 (ROP) ya existente. `architecture-decisions.md` queda sin acción.
- SCC-DEC-08 (OQ-SCC-04 resuelta, aprobación humana 2026-08-16): tras `verdict: ready` + aprobación humana (`## Human Plan Approval: approved_by_user`), Task Decomposer crea un task board para las acciones documentales SCC-A1 a SCC-A7 y las descompone en tareas atómicas ejecutables. No se ejecutan las acciones documentales sin un task board previo; el estado superior del task board arranca en `todo`.

## Validator findings

Hallazgos de validación pre-ejecución, verificados post-ejecución mediante el task board `docs/specs/tasks/stab-closure-consolidation-task-board.md` (estado superior `done`; las siete tareas SCC-A1–A7 `done` con `verification_result: PASS`, evidencia en disco registrada 2026-08-16). No se emitió un nuevo veredicto del Spec Validator post-ejecución; la verificación post-ejecución corresponde al task board. El `## Spec Validator Approval` (`verdict: ready`, 2026-08-16) se conserva como trazabilidad histórica del contrato pre-ejecución.

- Verificado (SCC-A1, PASS): el lifecycle del delta spec y shared context de estabilización se alineó a `closed` conservando las aprobaciones históricas como trazabilidad.
- Verificado (SCC-A2, PASS): B1/B3/B5 consolidados en Master Spec de forma durable y concisa sin duplicar historial STAB-* y sin fijar `iss`/`aud`/`typ`/proveedor JWT.
- Verificado (SCC-A4, PASS): TD-NEW-ROP-SIGN/HTTP-SEC/COV registradas como `active` (no resueltas) en ambos registros de deuda con contenido idéntico.
- Verificado (SCC-A5, PASS): TD-NEW-STAB-LIFECYCLE registrada como `resolved-by-verified-implementation` con evidencia (task board `done`, B1-B5 verificados, TD-MSF-STAB-001/002/003/004 resueltas, SCC-A1 aplicado).
- Verificado (SCC-A7, PASS): TD-MSF-ID-002-02/03 permanecen `active` como gates de producción; no se declaran resueltos.
- Verificado (global, PASS): ninguna acción modificó código, OpenAPI, Prisma, `package.json`, `.env*` ni runtime config.
- Verificado (global, PASS): no se ejecutaron operaciones de Git; la frescura de Graphify permanece **blocked para frescura**.
- Verificado (SCC-DEC-06, PASS): no se creó `docs/specs/archive/`; los artefactos de estabilización permanecen in-place marcados `closed`/históricos.
- Verificado (SCC-DEC-07, PASS): no se reeditó ADR-017; la consolidación de B1-B5 ocurre solo en Master Spec.
- Verificado (SCC-DEC-08, PASS): el gate de ejecución documental exigió task board previo de Task Decomposer para SCC-A1–A7; el task board fue creado y ejecutado con estado superior `done`.

## Resolved findings

- Todos los hallazgos de validación fueron verificados post-ejecución mediante el task board `docs/specs/tasks/stab-closure-consolidation-task-board.md` (estado superior `done`; las siete tareas SCC-A1–A7 `done` con `verification_result: PASS`, evidencia en disco registrada 2026-08-16). Las acciones documentales SCC-A1–A7 están implementadas y verificadas en disco: (SCC-A1) lifecycle del predecesor alineado a `closed`; (SCC-A2) B1/B3/B5 consolidados en Master Spec §ROP/§Identidad; (SCC-A3) artefactos in-place `closed`/históricos sin archive; (SCC-A4) TD-NEW-ROP-SIGN/HTTP-SEC/COV registradas `active`; (SCC-A5) TD-NEW-STAB-LIFECYCLE `resolved-by-verified-implementation`; (SCC-A6) workspace_changes actualizado; (SCC-A7) gates legal/AWS confirmados `active`. No se modificó código, OpenAPI, Prisma, `package.json`, runtime config ni se ejecutó Git.

## Open questions

- Ninguna abierta. OQ-SCC-01, OQ-SCC-02, OQ-SCC-03 y OQ-SCC-04 están resueltas por aprobación humana (2026-08-16) y registradas como decisiones locked: OQ-SCC-01 → SCC-DEC-01 (confirmada, estado final `closed`); OQ-SCC-02 → SCC-DEC-06 (mantener in-place, sin archive); OQ-SCC-03 → SCC-DEC-07 (consolidar solo en Master Spec, sin reeditar ADR-017); OQ-SCC-04 → SCC-DEC-08 (Task Decomposer crea task board de SCC-A1–A7).

## Stale terms guard

Forbidden: declarar TD-NEW-ROP-SIGN/HTTP-SEC/COV resueltas (son `active`); declarar TD-MSF-ID-002-02/03 resueltas (son gates abiertos); fijar `iss`/`aud`/`typ` o el proveedor JWT canónicamente como decisión vigente (son decisión pendiente STAB-DEC-12; la implementación `jsonwebtoken`/HS256 sin `iss`/`aud` es provisional y no constituye la decisión canónica); afirmar que la consolidación de B3/B5 cubre `iss`/`aud`/`typ` o el proveedor JWT (B3 cubre solo fail-fast del secreto; B5 cubre solo traducción ROP de `jwt.verify`); afirmar que el grafo está actualizado respecto a `HEAD` sin verificación con Git (blocked para frescura); modificar código (`projects/merkee-shop-api/src/**`), OpenAPI (`docs/api/openapi.yaml`), Prisma (`projects/merkee-shop-api/prisma/**`), `package.json`, `.env*` ni runtime config; ejecutar Git (commits, branches, PRs); crear task board antes de `verdict: ready` + aprobación humana (`## Human Plan Approval: approved_by_user`); declarar `ready` sin Spec Validator; marcar el incremento de estabilización como `superseded` (no fue reemplazado por una decisión distinta; es `closed`); eliminar las aprobaciones históricas `verdict: ready`/`## Human Plan Approval: approved_by_user`/`## Controlled Pre-Ready Remediation: approved_by_user` del incremento de estabilización (se conservan como trazabilidad histórica); crear endpoints, migraciones, schemas, parámetros o reglas de negocio; declarar listo para producción (gates legal/AWS abiertos); `try/catch` técnico en `application` como patrón vigente (es deuda TD-NEW-ROP-SIGN, active); default de JWT en producción como patrón vigente (B3 lo corrige con fail-fast, pero TD-NEW-HTTP-SEC mantiene deuda de headers HTTP); `coverageThreshold` ausente como estado aceptado (es deuda TD-NEW-COV, active); mezclar `implemented` con `closed` sin distinguir (B1-B5 implementados → incremento cerrado/consolidado por este incremento); crear `docs/specs/archive/` o mover artefactos de estabilización a ella (SCC-DEC-06: mantener in-place); reeditar ADR-017 en `architecture-decisions.md` (SCC-DEC-07: consolidar solo en Master Spec); ejecutar acciones documentales SCC-A1–A7 sin task board previo de Task Decomposer (SCC-DEC-08).

## Next action

Incremento `stab-closure-consolidation` cerrado (2026-08-16). Las siete acciones documentales SCC-A1–A7 están implementadas y verificadas en disco mediante el task board `docs/specs/tasks/stab-closure-consolidation-task-board.md` (estado superior `done`; las siete tareas `done` con `verification_result: PASS`). El `## Spec Validator Approval` (`verdict: ready`) y la `## Human Plan Approval: approved_by_user` se conservan como trazabilidad histórica del contrato pre-ejecución.

**Incremento siguiente:** planificar el siguiente incremento de deuda técnica, priorizando **TD-NEW-ROP-SIGN** (alinear `register`/`login`/`refresh-session` al patrón ROP, continuación natural de STAB-B1 para `activate-admin`), seguido de **TD-NEW-HTTP-SEC** (headers de seguridad HTTP/CORS/CSRF/rate-limit en `main.ts`) y **TD-NEW-COV** (`coverageThreshold` en `package.json`/Jest). Las tres deudas permanecen `active` (no resueltas en este incremento). La consolidación final de los registros global/local de deuda se realizará después de terminar los incrementos de deuda técnica (TD-NEW-ROP-SIGN/HTTP-SEC/COV), conforme a la aprobación humana registrada.

**Permanecen abiertos (no se cierran en este incremento):** los gates de producción TD-MSF-ID-002-02 (legal/contable) y TD-MSF-ID-002-03 (AWS); la decisión JWT STAB-DEC-12 (`iss`/`aud`/`typ`/proveedor) sigue pendiente para una delta futura antes de producción; la frescura de Graphify respecto a `HEAD` permanece **blocked para frescura** (no se ejecutó Git). No se declara listo para producción.
