# Shared Context — merkee-shop-foundation-stabilization

**Lifecycle status:** `closed`
**Actualizado:** 2026-08-16 (cierre por incremento `stab-closure-consolidation`, SCC-A1)

## Current status
Current status: closed. El incremento fue `implemented` (B1-B5 verificados en disco 2026-08-16) y transiciona a `closed` al ser consolidado por el incremento `stab-closure-consolidation` (SCC-A1, 2026-08-16). B1-B5 (STAB-B1 ROP activate-admin, STAB-B2 must_change_password replay `true`, STAB-B3 JWT fail-fast del secreto, STAB-B4 operation-map 404, STAB-B5 ROP JWT verify) están implementadas y verificadas en disco (2026-08-16). TD-MSF-STAB-001/002/003/004 marcadas `resolved-by-verified-implementation` en `technical_debt.md` (global y espejo local) y `workspace_changes.md`. Los gates de producción TD-MSF-ID-002-02 (legal/contable) y TD-MSF-ID-002-03 (AWS) permanecen abiertos; no se declara listo para producción. La decisión JWT (`iss`/`aud`/`typ` y proveedor) sigue pendiente para una delta futura (STAB-DEC-12). El texto histórico de planificación que sigue queda como trazabilidad.

`planning`: remediación documental (A1-A7) **aplicada y verificada en disco** el 2026-08-16: STAB-A1 (ruta Prisma corregida en `prisma-migration-contract.md`), STAB-A2 (prefijo `/v1` aclarado en Master Spec Decomposition Contract), STAB-A3 (cifras Graphify corregidas a 1,245 nodos / 2,603 aristas en shared context de `merkee-shop-foundation`), STAB-A4 (deudas TD-MSF-STAB-001/002/003 sincronizadas en `workspace_changes.md`; ya existían en `technical_debt.md` global y local), STAB-A5 (AC-05 ampliado con Opción A `must_change_password: true` en replay), STAB-A6 (§NC-08 aclarado: scheduler es driving adapter local con `setTimeout`, no cron literal), STAB-A7 (10 extensiones `x-idempotency` añadidas en `openapi.yaml` para operaciones admin/media con `Idempotency-Key`; no cambia contrato de comunicación). **Drifts STAB-DRIFT-01/02 resueltos por decisión del usuario (Opción A, 2026-08-16) y aplicados en disco:** STAB-DRIFT-01 (404 de provision: `404 RESOURCE_NOT_FOUND` añadido a OpenAPI `provisionAdminUser` + aclaración Master Spec §ROP; cambio de contrato de comunicación autorizado) y STAB-DRIFT-02 (webhook duplicate semantics: descripción del `204` aclarada en OpenAPI de los webhooks + aclaración Master Spec §ROP de que `DUPLICATE_WEBHOOK_EVENT` 409 es clasificación interna no proyectada al proveedor; sin cambio de statuses). Las correcciones de código (B1 ROP activate-admin, B2 must_change_password, B3 JWT fail-fast del secreto, B4 operation-map 404, B5 ROP JWT verify) permanecen **pendientes como código** (no se tocó código; son propiedad del Executor, no deuda técnica). **B1-B5 son tareas pendientes de implementación con criterios de aceptación y evidencia futura; NO son precondición para que Spec Validator evalúe el contrato de este incremento ni se exigen resueltas antes de solicitar la revalidación.** El gate de Spec Validator evalúa consistencia, decisiones, alcance y descomponibilidad del contrato (specs/OpenAPI/migraciones/ADRs/shared context), no que el código ya esté corregido. B1-B5 permanecen no resueltas y se ejecutan tras veredicto `ready` + aprobación humana formal (`## Human Plan Approval: approved_by_user`). **STAB-B5 (nuevo, hallazgo formal de Spec Validator, autorización del usuario 2026-08-16):** `JwtAdapter.verify` propaga excepciones de `jwt.verify` (`TokenExpiredError`/`JsonWebTokenError`/`NotBeforeError`) sin traducir a `DomainError`; `JwtPort.verify` devuelve `Promise<JwtPayload>` (no `Result<JwtPayload, DomainError>`); el controller `extractSessionId`/`extractUserId` inyecta `JwtAdapter` concreto (no el puerto `JwtPort`) y captura la excepción con `try/catch`. Contradice Master Spec §ROP y ADR-017. Corrección: el adapter traduce fallos técnicos a `DomainError` del catálogo existente (token inválido/expirado → `AUTHENTICATION_REQUIRED` 401; error técnico inesperado → `TECHNICAL_DEPENDENCY_FAILURE` 500) sin causa/PII; el puerto devuelve `Result`; el controller consume `Result` del puerto sin `try/catch`. STAB-B5 cubre **solo** la traducción ROP de `jwt.verify`; **no cubre** `iss`/`aud`/`typ` ni el proveedor JWT (siguen como decisión pendiente STAB-DEC-12, no se eligen en este incremento) ni el fail-fast del secreto (B3). La deuda técnica (TD-MSF-ID-002-01/02/03, TD-MSF-API-001..007, TD-MSF-STAB-001/002/003) permanece activa como gates de producción o deuda persistente, separada del código pendiente. La evidencia Graphify (`graphify-out/GRAPH_REPORT.md`) tiene contenido verificado en disco (1,245 nodos / 2,603 aristas, sin ciclos) pero frescura respecto a `HEAD` no verificable sin Git (**blocked para frescura**). La decisión sobre valores `iss`/`aud`/`typ` y proveedor JWT (algoritmo/biblioteca) queda **pendiente de decisión**, no fijada canónicamente (ver `## Open questions`). No se declara `ready`; Spec Validator permanece `pending`. No hay handoff. Las decisiones STAB-DEC-10/11 (Opción A) están registradas y bloqueadas. **Remediación controlada previa al veredicto `ready` autorizada por el usuario (2026-08-16), limitada exclusivamente a STAB-B1/B2/B3/B4**, para romper el bloqueo circular validator↔implementation — ver `## Controlled Pre-Ready Remediation: approved_by_user`. Esta autorización es estrictamente documental: no declara `ready`, no hace handoff, no autoriza nuevas funcionalidades, task board general, producción ni Git. Spec Validator permanece `pending`; lifecycle permanece `planning`.

## Canonical artifacts

- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/increments/merkee-shop-foundation-stabilization-delta-spec.md` (esta delta)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/master_spec.md` (`revision-needed`)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/api/openapi.yaml` (canónico; cambios aprobados aplicados: `404` en `provisionAdminUser` STAB-DRIFT-01 Opción A, descripción del `204` aclarada en webhooks STAB-DRIFT-02 Opción A, 10 extensiones `x-idempotency` STAB-A7)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/prisma-migration-contract.md` (STAB-A1 aplicado: ruta canónica corregida a `projects/merkee-shop-api/prisma/`)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/architecture-decisions.md` (ADR-017 ROP, ADR-018)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/.working/merkee-shop-foundation-sdd-context.md` (STAB-A3 aplicado: cifras Graphify corregidas a 1,245 nodos / 2,603 aristas)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/tasks/merkee-shop-foundation-task-board.md` (`blocked`)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/workspace_changes.md` (STAB-A4 aplicado: deudas de estabilización sincronizadas; drifts STAB-DRIFT-01/02 resueltos Opción A)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/technical_debt.md` (STAB-A4 aplicado: TD-MSF-STAB-001/002/003 registradas)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/projects/merkee-shop-api/docs/specs/technical_debt.md` (espejo local)
- `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/graphify-out/GRAPH_REPORT.md` (construido desde commit `89bcd155`; frescura respecto a HEAD no verificable sin Git — **blocked para frescura**; cifras 1,245 nodos / 2,603 aristas verificadas en disco)

## Artifact evidence

| Ruta | Campo/flujo verificado | Resultado | Estado |
|---|---|---|---|
| `docs/api/openapi.yaml` línea 384 | `AdminUserProvisionResponse.must_change_password: { type: boolean, const: true }` | OpenAPI canónico fija `const: true`; el código replay ahora fuerza `true` (B2 aplicado y verificado). | pass |
| `src/contract/schemas.ts` línea 249 | `must_change_password: true` (literal) | Alineado a OpenAPI `const: true` (B2 aplicado y verificado en disco). | pass |
| `src/modules/identity/application/use-cases/provision-admin-user.use-case.ts` línea 284 | `must_change_password: true` en `replayFromResource` | Replay fuerza `true` (constante de provisión); alineado a OpenAPI `const: true` (B2 aplicado y verificado en disco). | pass |
| `src/modules/identity/application/use-cases/activate-admin.use-case.ts` | Sin `try/catch` técnico en application; consume `Result` del puerto (`outcome.ok`/`outcome.error`) | ROP correcto: application sin captura técnica (B1 aplicado y verificado en disco). | pass |
| `src/modules/identity/domain/ports/activate-admin-unit-of-work.port.ts` línea 37 | `run<T>(...): Promise<Result<T, DomainError>>` | Devuelve `Result`; alineado al patrón `ProvisionUnitOfWorkPort` (B1 aplicado y verificado en disco). | pass |
| `src/modules/identity/infrastructure/adapters/prisma-activate-admin-unit-of-work.adapter.ts` líneas 38-54 | `try { ... } catch { ... return fail(technicalFailure()); }` | Captura y traduce a `fail(technicalFailure())` sin causa/PII (B1 aplicado y verificado en disco). | pass |
| `src/modules/identity/infrastructure/adapters/prisma-provision-unit-of-work.adapter.ts` | `run` devuelve `Result`, captura y traduce a `fail(technicalFailure())` | Patrón ROP correcto; referencia para STAB-B1. | pass |
| `src/modules/identity/infrastructure/adapters/jwt.adapter.ts` líneas 24-37 | Fail-fast en `NODE_ENV=production` si `JWT_SECRET` ausente o < 32 bytes; default solo en desarrollo con advertencia | Fail-fast de seguridad aplicado (B3 aplicado y verificado en disco); `.env.example` documenta `JWT_SECRET`. | pass |
| `src/modules/identity/domain/ports/jwt.port.ts` línea 25 | `verify(token: string): Promise<Result<JwtPayload, DomainError>>` | Devuelve `Result`; alineado a Master Spec §ROP (B5 aplicado y verificado en disco). | pass |
| `src/modules/identity/infrastructure/adapters/jwt.adapter.ts` líneas 46-66 | `verify` captura `TokenExpiredError`/`JsonWebTokenError`/`NotBeforeError` y traduce a `sessionNotFoundOrExpired()` (`AUTHENTICATION_REQUIRED` 401) o `technicalFailure()` (`TECHNICAL_DEPENDENCY_FAILURE` 500) sin causa/PII | ROP correcto: adapter traduce excepciones a `DomainError` del catálogo existente (B5 aplicado y verificado en disco). `sign` no afectado (B5 es solo `verify`). | pass |
| `src/modules/identity/identity.controller.ts` líneas 52, 232-233, 243-244 | `@Inject(IDENTITY_TOKENS.JWT) private readonly jwtPort: JwtPort`; `extractSessionId`/`extractUserId` consumen `Result` del puerto (`result.ok ? result.value.session_id : undefined`) sin `try/catch` | ROP correcto: controller inyecta `JwtPort` (no `JwtAdapter` concreto) y consume `Result` sin `try/catch` (B5 aplicado y verificado en disco). | pass |
| `docs/specs/prisma-migration-contract.md` línea 6 | Ruta canónica Prisma | **Aplicado (STAB-A1):** corregida a `projects/merkee-shop-api/prisma/schema.prisma` y `migrations/`; verificada en disco; referencia histórica `apps/api/` marcada SUPERSEDED. | pass |
| `docs/specs/master_spec.md` §Decomposition Contract | Rutas con `/v1` en el path | **Aplicado (STAB-A2):** aclarado que los paths canónicos son sin `/v1` (OpenAPI) y que `/v1` es prefijo del `servers.url`; las referencias con `/v1` son rutas completas legibles. | pass |
| `docs/specs/.working/merkee-shop-foundation-sdd-context.md` línea 32 | Cifras Graphify | **Aplicado (STAB-A3):** corregidas a 1,245 nodos / 2,603 aristas (verificadas en `GRAPH_REPORT.md`); commit `89bcd155` es el commit de construcción del grafo, pero la igualdad con HEAD no es verificable sin Git (blocked para frescura). | pass (cifras) / blocked (frescura) |
| `docs/specs/master_spec.md` AC-05 | Replay de provisión `must_change_password` | **Aplicado (STAB-A5):** AC-05 ampliado: replay devuelve `must_change_password: true` (Opción A), consistente con OpenAPI `const: true`. | pass |
| `docs/specs/master_spec.md` §NC-08 | Semántica del scheduler | **Aplicado (STAB-A6):** aclarado: driving adapter local (`OnApplicationBootstrap`, `setTimeout`, configurable), no cron literal. | pass |
| `docs/api/openapi.yaml` (10 operaciones admin/media) | `x-idempotency` faltante | **Aplicado (STAB-A7):** 10 extensiones `x-idempotency` añadidas (adminCreateCategory, adminUpdateCategory, adminDeleteCategory, adminCreateProduct, adminUpdateProduct, adminDeleteProduct, adminCreateBanner, adminUpdateBanner, adminDeleteBanner, createMediaUploadUrl); no cambia contrato de comunicación; operation-map ya marcaba `idempotencyKey: true`. | pass |
| `docs/specs/workspace_changes.md` | Deudas de estabilización | **Aplicado (STAB-A4):** TD-MSF-STAB-001/002/003 añadidas a la tabla de deuda activa; STAB-DRIFT-01/02 registrados inicialmente como `decision-required` y **posteriormente resueltos por Opción A del usuario (2026-08-16)** — ver filas STAB-DRIFT-01/02 abajo y `workspace_changes.md` punto 4. | pass |
| `docs/api/openapi.yaml` `provisionAdminUser` responses | Status 404 añadido | **Aplicado (STAB-DRIFT-01 Opción A, aprobada usuario 2026-08-16):** OpenAPI ahora declara `'404': { $ref: '#/components/responses/NotFound', description: '404 RESOURCE_NOT_FOUND en replay idempotente...' }` en `provisionAdminUser` (línea 205); el código ya emitía `RESOURCE_NOT_FOUND` (404); Master Spec §ROP aclaró el 404 en replay. `operation-map.ts` (STAB-B4) pendiente de actualización de código. | pass |
| `docs/api/openapi.yaml` `receiveWompiWebhook`/`receiveMercadoPagoWebhook` responses | 204 "Aceptado o duplicado" aclarado | **Aplicado (STAB-DRIFT-02 Opción A, aprobada usuario 2026-08-16):** descripción del `204` aclarada en líneas 343 y 354 ("Aceptado o duplicado idempotente; los eventos duplicados reciben 204 (no 409) y se clasifican internamente como `DUPLICATE_WEBHOOK_EVENT` sin proyectarse al proveedor"); Master Spec §ROP aclaró que `DUPLICATE_WEBHOOK_EVENT` 409 es clasificación interna; sin cambio de statuses (204/400/401/500). | pass |
| `src/shared/http/domain-error-mapper.ts` | Tabla canónica código→status | Coincide con Master Spec §ROP; degrada desconocidos a 500. | pass |
| `src/shared/http/transport-validation.pipe.ts` | 400 sintaxis `INVALID_DOMAIN_INPUT` | Alineado con Master Spec §59. | pass |
| `src/modules/identity/infrastructure/adapters/scheduled-idempotency-purge.adapter.ts` | Driving adapter `OnApplicationBootstrap`, `setTimeout` + hora `HH:MM` UTC (defecto `02:00`), configurable, idempotente | Cableado local correcto (no cron literal); AWS es decisión posterior TD-MSF-ID-002-03. | pass |
| `graphify-out/GRAPH_REPORT.md` | Sin ciclos de importación; 269 nodos aislados | Grafo construido desde commit `89bcd155`; frescura respecto a HEAD no verificable sin Git (blocked para frescura); cifras 1,245 nodos / 2,603 aristas verificadas en disco. | pass (contenido) / blocked (frescura) |

## Spec Validator Approval

verdict: ready
reviewed_at: 2026-08-16T00:00:00-05:00
validator_agent: spec-validator
artifact_set_reviewed: /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/increments/merkee-shop-foundation-stabilization-delta-spec.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/master_spec.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/api/openapi.yaml; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/prisma-migration-contract.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/architecture-decisions.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/.working/merkee-shop-foundation-stabilization-sdd-context.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/workspace_changes.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/technical_debt.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/projects/merkee-shop-api/docs/specs/technical_debt.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/requirements/merkee-shop-foundation-requirements-brief.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/docs/specs/tasks/merkee-shop-foundation-task-board.md; /home/cristiansrc/Documentos/Proyectos/merkee-workspace/graphify-out/GRAPH_REPORT.md
summary: Contrato consistente, cerrado, acotado y descomponible. B1-B5 permanecen pendientes de implementación y no son precondición; issuer/audience/typ y proveedor JWT siguen pendientes para una delta futura. Graphify tiene cifras verificadas, con frescura respecto a HEAD blocked.
invalidated_by_changes_since: none

## Human Plan Approval: approved_by_user

**Aprobación del usuario:** 2026-08-16 · **Alcance:** STAB-B1, STAB-B2, STAB-B3, STAB-B4, STAB-B5 (exhaustivo y exclusivo) · **Autoriza a:** Task Decomposer (descomposición) y Executor (implementación) exclusivamente para las correcciones de código del incremento de estabilización B1-B5 (ROP activate-admin, `must_change_password` replay `true`, JWT fail-fast del secreto, `operation-map.ts` 404, ROP JWT verify). · **No autoriza:** (1) nuevas funcionalidades —endpoints, módulos, migraciones, schemas, parámetros o reglas de negocio nuevos—; (2) el desbloqueo del task board general, que permanece `blocked` (TD-MSF-API-006); (3) despliegue u operación en producción (gates TD-MSF-ID-002-02 legal/contable y TD-MSF-ID-002-03 AWS permanecen); (4) operaciones de Git (commits, branches, PRs); (5) la modificación de código, Prisma, `package.json`, runtime ni otros artefactos por parte del Planner. Esta aprobación se circunscribe estrictamente al alcance B1-B5 y no extiende autorización a ningún otro agente, artefacto, flujo o decisión fuera de dicho alcance. La remediación controlada documental previa (`## Controlled Pre-Ready Remediation: approved_by_user`) y el veredicto `ready` de Spec Validator (2026-08-16) preceden a esta aprobación humana; ambas condiciones previas se cumplen.

## Controlled Pre-Ready Remediation: approved_by_user

**Autorización del usuario:** 2026-08-16 · **Alcance:** STAB-B1, STAB-B2, STAB-B3, STAB-B4, STAB-B5 (exhaustivo y exclusivo) · **Tipo:** remediación controlada documental previa al veredicto `ready` del Spec Validator · **Estado del gate:** Spec Validator permanece `pending`/`not ready`; lifecycle del incremento permanece `planning`.

### Trazabilidad — bloqueo circular validator↔implementation resuelto

Esta sección documenta como trazabilidad el bloqueo circular identificado por Spec Validator y cómo se resolvió a nivel de contrato (no operativo):
- **Bloqueo identificado:** el gate de Spec Validator exigía que los drifts de código STAB-B1/B2/B3/B4/B5 estuvieran corregidos antes de emitir `ready`, pero el Planner no puede enrutar B1-B5 a Executor/Task Decomposer sin un veredicto `ready` vigente (regla del sistema: "Planner no debe hacer handoff a Task Decomposer, Executor ni Architect Executor salvo que el último veredicto de Spec Validator sea exactamente `ready`").
- **Resolución a nivel de contrato (no bypass operativo):** se redefinió el gate de Spec Validator para que evalúe consistencia, decisiones, alcance y descomponibilidad del contrato del incremento, no que el código B1-B5 ya esté corregido. B1-B5 son tareas pendientes de implementación con criterios de aceptación y evidencia futura; se ejecutan tras `ready` + aprobación humana formal. Esto elimina el ciclo sin eludir el gate: la revalidación focalizada de Spec Validator sigue siendo obligatoria, pero evalúa el contrato, no la implementación.

El heading `## Controlled Pre-Ready Remediation: approved_by_user` se conserva solo como trazabilidad del bloqueo resuelto; no constituye un bypass operativo, no declara `ready` y no sustituye la revalidación focalizada de Spec Validator.

### Alcance exacto (exhaustivo y exclusivo)

| ID | Corrección de código | Archivos autoritativos (verificados en disco el 2026-08-16; NO modificados por el Planner) |
|---|---|---|
| STAB-B1 | ROP activate-admin: puerto `ActivateAdminUnitOfWorkPort.run` → `Result<T, DomainError>`; adapter `PrismaActivateAdminUnitOfWorkAdapter` captura/traduce a `fail(technicalFailure())` sin causa/PII; `activate-admin.use-case.ts` sin `try/catch` técnico; DI en `identity.module.ts`. | `projects/merkee-shop-api/src/modules/identity/domain/ports/activate-admin-unit-of-work.port.ts`; `projects/merkee-shop-api/src/modules/identity/infrastructure/adapters/prisma-activate-admin-unit-of-work.adapter.ts`; `projects/merkee-shop-api/src/modules/identity/application/use-cases/activate-admin.use-case.ts`; `projects/merkee-shop-api/src/modules/identity/identity.module.ts` |
| STAB-B2 | `must_change_password` en replay: `provision-admin-user.use-case.ts` fuerza `true` en `replayFromResource`; `schemas.ts` tipa `must_change_password: true` (literal); OpenAPI `const: true` sin cambio (STAB-DEC-01 Opción A). | `projects/merkee-shop-api/src/modules/identity/application/use-cases/provision-admin-user.use-case.ts`; `projects/merkee-shop-api/src/contract/schemas.ts` |
| STAB-B3 | JWT fail-fast: `jwt.adapter.ts` lanza error de arranque en `NODE_ENV=production` si `JWT_SECRET` ausente o < 32 bytes; default solo en desarrollo con advertencia; `.env.example` documenta `JWT_SECRET`. | `projects/merkee-shop-api/src/modules/identity/infrastructure/adapters/jwt.adapter.ts`; `projects/merkee-shop-api/.env.example` |
| STAB-B4 | `operation-map.ts` actualiza el mapa de trazabilidad `provisionAdminUser` para reflejar el `404` añadido en OpenAPI (statuses: 201,400,401,403,404,409,429,500). | `projects/merkee-shop-api/src/contract/operation-map.ts` |
| STAB-B5 | ROP JWT verify: `JwtPort.verify` → `Result<JwtPayload, DomainError>`; `JwtAdapter.verify` captura excepciones de `jwt.verify` (`TokenExpiredError`/`JsonWebTokenError`/`NotBeforeError`) en su límite y traduce a `DomainError` del catálogo existente (token inválido/expirado → `AUTHENTICATION_REQUIRED` 401, consistente con `sessionNotFoundOrExpired()`; error técnico inesperado → `TECHNICAL_DEPENDENCY_FAILURE` 500) sin causa/PII, devuelve `fail(...)`; el controller `extractSessionId`/`extractUserId` consume `Result` del puerto `JwtPort` (no del adapter concreto `JwtAdapter`), elimina el `try/catch` y maneja el rail `Failure` (devolviendo `undefined` para token inválido, consistente con el comportamiento actual); DI en `identity.module.ts` (controller inyecta `JwtPort`, no `JwtAdapter`). **No cubre** `iss`/`aud`/`typ` ni el proveedor JWT (decisión pendiente STAB-DEC-12) ni el fail-fast del secreto (B3). | `projects/merkee-shop-api/src/modules/identity/domain/ports/jwt.port.ts`; `projects/merkee-shop-api/src/modules/identity/infrastructure/adapters/jwt.adapter.ts` (compartido con B3 — hallazgos distintos: B3 fail-fast secreto, B5 traducción `verify`); `projects/merkee-shop-api/src/modules/identity/identity.controller.ts`; `projects/merkee-shop-api/src/modules/identity/identity.module.ts` |

### Límites

- Esta remediación controlada se limita **exclusivamente** a STAB-B1, STAB-B2, STAB-B3, STAB-B4 y STAB-B5. Cualquier otro hallazgo o corrección (incluida la deuda técnica C y los gates de producción) queda fuera de alcance.
- El Planner solo actualiza **documentación** del incremento de estabilización (este shared context, la delta spec `merkee-shop-foundation-stabilization-delta-spec.md` y `docs/specs/workspace_changes.md`). El Planner no crea, edita ni parchea código, Prisma, `package.json`, runtime, tests, migraciones, scripts, UI, API handlers, queries ni automatización.
- Las correcciones de código B1-B5 son propiedad del Executor; el Planner no las ejecuta ni las descompone en tareas (Task Decomposer es dueño de la descomposición tras el gate).
- Esta autorización es una remediación controlada **previa al veredicto `ready`**: no constituye un veredicto `ready`, no sustituye la revalidación focalizada de Spec Validator y no elimina el gate de aprobación humana (`## Human Plan Approval: approved_by_user`).

### Prohibiciones

- **No declarar `ready`**: Spec Validator permanece `pending`/`not ready`; el lifecycle del incremento de estabilización permanece `planning` y el de `workspace_changes.md` permanece `revision-needed`.
- **No hacer handoff** a Task Decomposer, Executor ni Architect Executor sin veredicto `ready` de Spec Validator + aprobación humana (`## Human Plan Approval: approved_by_user`).
- **No autorizar nuevas funcionalidades**: no se crean endpoints, módulos, migraciones, schemas, parámetros, reglas de negocio ni integraciones nuevas.
- **No autorizar task board general**: el task board global permanece `blocked` (TD-MSF-API-006); esta remediación no desbloquea tareas fuera de B1-B5.
- **No autorizar producción**: los gates TD-MSF-ID-002-02 (legal/contable) y TD-MSF-ID-002-03 (AWS) permanecen; no se despliega ni se opera en producción.
- **No autorizar Git**: no se ejecutan commits, branches, PRs ni operaciones de Git; la frescura de Graphify respecto a `HEAD` permanece **blocked para frescura**.
- **No modificar** código, Prisma (`schema.prisma`, migraciones), `package.json`, runtime config, `.env` real, ni ningún archivo de implementación.

### Evidencia futura requerida (para cerrar B1-B5 tras aprobación; no habilita ni es precondición para la revalidación de Spec Validator)

- STAB-B1: test estático sin `try/catch` técnico en `application`; test del adapter traduciendo fallo técnico a `TECHNICAL_DEPENDENCY_FAILURE` sin causa/PII; `npm run depcruise` sin violaciones.
- STAB-B2: test de replay tras activación devuelve `must_change_password: true`; diff de `schemas.ts` con literal `true`; OpenAPI `const: true` sin cambio.
- STAB-B3: test de fail-fast en producción (`NODE_ENV=production`, `JWT_SECRET` ausente o < 32 bytes); diff de `.env.example` con `JWT_SECRET` documentado.
- STAB-B4: diff de `operation-map.ts` con `provisionAdminUser` statuses `201,400,401,403,404,409,429,500` consistente con OpenAPI.
- STAB-B5: `JwtPort.verify` devuelve `Result<JwtPayload, DomainError>`; test del adapter traduciendo `TokenExpiredError`/`JsonWebTokenError`/`NotBeforeError` a `DomainError` del catálogo existente (`AUTHENTICATION_REQUIRED` 401 para token inválido/expirado; `TECHNICAL_DEPENDENCY_FAILURE` 500 para error técnico inesperado) sin causa/PII; el controller `extractSessionId`/`extractUserId` consume `Result` del puerto `JwtPort` (no del adapter concreto) sin `try/catch`; DI actualizada en `identity.module.ts` (controller inyecta `JwtPort`); `npm run depcruise` sin violaciones (controller→`JwtPort`, no →`JwtAdapter` concreto).
- Global: `npm run build` OK; `npm test` PASS con suites actualizadas (activate-admin, provision replay, JWT fail-fast, JWT verify ROP); `npm run depcruise` sin violaciones; sin `HttpException` ni `try/catch` técnico en `domain`/`application`; el controller no captura excepciones técnicas de `jwt.verify` (consume `Result`).

### Lo que esta autorización NO autoriza

Esta remediación controlada **no autoriza**: (1) nuevas funcionalidades, endpoints, módulos, migraciones, schemas, parámetros ni reglas de negocio; (2) el desbloqueo del task board general (permanece `blocked`); (3) despliegue u operación en producción; (4) operaciones de Git (commits, branches, PRs); (5) la modificación de código, Prisma, `package.json` ni runtime por parte del Planner; (6) handoff a otros agentes sin veredicto `ready` + aprobación humana; (7) declarar `ready` ni transicionar el incremento fuera de `planning`/`revision-needed`.

## Decisions locked

- STAB-DEC-01: Opción A — OpenAPI mantiene `must_change_password: const: true`; el replay devuelve `true` (constante de provisión); no se cambia OpenAPI; el código se corrige (B2). Alternativa Opción B (OpenAPI `boolean`) rechazada y documentada; revertir B2 si el usuario la prefiere.
- STAB-DEC-02: paths canónicos sin prefijo `/v1` (OpenAPI); `/v1` es prefijo del `servers.url`; las referencias de Master Spec con `/v1` son rutas completas legibles.
- STAB-DEC-03: Graphify construido desde commit `89bcd155`; las cifras (1,245 nodos / 2,603 aristas) se corrigieron en el shared context de `merkee-shop-foundation` y coinciden con `GRAPH_REPORT.md` en disco. La frescura respecto a HEAD no es verificable sin ejecutar Git (prohibido en esta sesión); Graphify se registra como **blocked para frescura**. Recomendar ejecutar `graphify update .` y `git rev-parse HEAD` (comparar con el commit del reporte) antes de la revalidación de Spec Validator como verificación defensiva.
- STAB-DEC-04: ROP activate-admin alineado al patrón de provision/bootstrap (puerto devuelve `Result`, adapter traduce, application sin `try/catch` técnico).
- STAB-DEC-05: JWT fail-fast en producción si `JWT_SECRET` ausente o < 32 bytes; default solo en desarrollo con advertencia; documentar en `.env.example`.
- STAB-DEC-06: Scheduler local correctamente cableado como driving adapter (`ScheduledIdempotencyPurgeAdapter`, `OnApplicationBootstrap`, configurable vía `IDEMPOTENCY_PURGE_SCHEDULE_ENABLED`/`IDEMPOTENCY_PURGE_SCHEDULE_TIME` como hora `HH:MM` UTC, defecto `02:00`, actualmente con `setTimeout`; no un cron literal); AWS es decisión posterior (TD-MSF-ID-002-03, gate de producción); sin acción de código en este incremento.
- STAB-DEC-07 (nuevo): la corrección OpenAPI STAB-A7 (10 `x-idempotency` faltantes) es documental de consistencia de tooling; no cambia contrato de comunicación (endpoints/schemas/statuses/parámetros intactos); operation-map ya marcaba `idempotencyKey: true` para esas operaciones.
- STAB-DEC-08 (nuevo): la Master Spec AC-05 se amplió para fijar explícitamente que el replay de provisión devuelve `must_change_password: true` (Opción A), consistente con OpenAPI `const: true`; la activación posterior es un flujo separado.
- STAB-DEC-09 (nuevo): la Master Spec §NC-08 se aclaró: el scheduler materializa la semántica `0 0 2 * * *` con un driving adapter local (`OnApplicationBootstrap`, `setTimeout` + hora `HH:MM` UTC, configurable), no un cron literal.
- STAB-DEC-10 (nuevo, aprobado por el usuario 2026-08-16): STAB-DRIFT-01 Opción A — añadir `404 RESOURCE_NOT_FOUND` a OpenAPI `provisionAdminUser`. Cambio de contrato de comunicación (añade status 404) autorizado; el código ya emitía el 404 y ahora OpenAPI lo declara. Master Spec §ROP aclaró el 404 en replay idempotente. No se elimina el 404 del código. Corrección de código STAB-B4 (operation-map.ts) pendiente. Opción B (eliminar 404 del código) rechazada.
- STAB-DEC-11 (nuevo, aprobado por el usuario 2026-08-16): STAB-DRIFT-02 Opción A — el webhook HTTP devuelve `204` idempotente a duplicados (OpenAPI sin cambio de statuses) y `DUPLICATE_WEBHOOK_EVENT` (409) es clasificación interna del rail ROP no proyectada al proveedor. Aclaración documental aplicada en Master Spec §ROP y en la descripción del 204 de OpenAPI. No se añade 409 a los webhooks. No requiere corrección de código. Opción B (añadir 409 a webhooks) rechazada.
- STAB-DEC-13 (nuevo, hallazgo formal STAB-B5 de Spec Validator, autorización del usuario 2026-08-16): ROP JWT verify — `JwtAdapter.verify` propaga excepciones de `jwt.verify` (`TokenExpiredError`/`JsonWebTokenError`/`NotBeforeError`) sin traducir; `JwtPort.verify` devuelve `Promise<JwtPayload>` (no `Result`); el controller inyecta `JwtAdapter` concreto y captura con `try/catch`. Corrección: el adapter traduce fallos técnicos a `DomainError` del catálogo existente (token inválido/expirado → `AUTHENTICATION_REQUIRED` 401, consistente con `sessionNotFoundOrExpired()` de `identity-errors.ts`; error técnico inesperado → `TECHNICAL_DEPENDENCY_FAILURE` 500) sin causa/PII; el puerto devuelve `Result<JwtPayload, DomainError>`; el controller consume `Result` del puerto `JwtPort` (no del adapter concreto) sin `try/catch`. No se inventan códigos nuevos del catálogo (ADR-017). **No cubre** `iss`/`aud`/`typ` ni el proveedor JWT (decisión pendiente STAB-DEC-12, no se eligen en este incremento) ni el fail-fast del secreto (B3). `sign` no afectado (B5 es solo `verify`); el `try/catch` de los use cases `register`/`login`/`refresh-session` sobre `jwt.sign`/otros adapters es hallazgo separado, **no** parte de B5.

## Validator findings

**Nota de alcance:** los hallazgos B1-B5 a continuación son verificaciones de implementación futura (post-ejecución de B1-B5 por Executor), NO precondición para que Spec Validator emita veredicto `ready` sobre el contrato del incremento. El veredicto `ready` evalúa consistencia, decisiones, alcance y descomponibilidad del contrato; B1-B5 se ejecutan tras `ready` + aprobación humana formal.

- Revalidar que STAB-B1 deja `activate-admin.use-case.ts` sin `try/catch` técnico y el adapter traduce excepciones a `TECHNICAL_DEPENDENCY_FAILURE` sin causa/PII.
- Revalidar que STAB-B2 deja el replay de provisión devolviendo `must_change_password: true` y `schemas.ts` alineado a OpenAPI `const: true`.
- Revalidar que STAB-B3 hace fail-fast de JWT en producción y documenta `JWT_SECRET`.
- Revalidar que STAB-B4 actualiza `operation-map.ts` para reflejar el `404` añadido a `provisionAdminUser` en OpenAPI (statuses: 201,400,401,403,404,409,429,500), manteniendo consistencia operation-map↔OpenAPI.
- Revalidar que STAB-B5 deja `JwtPort.verify` devolviendo `Result<JwtPayload, DomainError>`; `JwtAdapter.verify` traduce `TokenExpiredError`/`JsonWebTokenError`/`NotBeforeError` a `DomainError` del catálogo existente (`AUTHENTICATION_REQUIRED` 401 / `TECHNICAL_DEPENDENCY_FAILURE` 500) sin causa/PII; el controller `extractSessionId`/`extractUserId` consume `Result` del puerto `JwtPort` (no del adapter concreto) sin `try/catch`; DI actualizada en `identity.module.ts`.
- Revalidar que STAB-A1/A2/A3/A4 corrigen los artefactos documentales sin cambiar endpoints ni comportamiento.
- Revalidar que STAB-DRIFT-01 (404 añadido a OpenAPI `provisionAdminUser`) y STAB-DRIFT-02 (descripción del 204 aclarada en webhooks + Master Spec §ROP) están aplicados en disco y son consistentes con el código existente.
- Confirmar que ninguna aprobación histórica `ready` o humana se interpreta como autorización para handoff mientras `verdict: pending`.

## Resolved findings

Hallazgos **documentales** resueltos y verificados en disco el 2026-08-16 (remediación documental pura; no requieren acción de código):

- **STAB-A1 (resuelto — documental):** ruta canónica Prisma corregida a `projects/merkee-shop-api/prisma/` en `prisma-migration-contract.md`; referencia histórica `apps/api/` marcada SUPERSEDED.
- **STAB-A2 (resuelto — documental):** prefijo `/v1` aclarado en Master Spec Decomposition Contract (paths canónicos sin `/v1`; `/v1` es prefijo del `servers.url`).
- **STAB-A3 (resuelto — documental, contenido):** cifras Graphify corregidas a 1,245 nodos / 2,603 aristas en el shared context de `merkee-shop-foundation`. **Frescura respecto a `HEAD` no verificable sin Git → blocked para frescura** (no es un hallazgo resuelto; ver categoría de evidencia abajo).
- **STAB-A4 (resuelto — documental):** deudas TD-MSF-STAB-001/002/003 sincronizadas en `workspace_changes.md`, `technical_debt.md` global y espejo local.
- **STAB-A5 (resuelto — documental):** AC-05 ampliado con Opción A `must_change_password: true` en replay.
- **STAB-A6 (resuelto — documental):** §NC-08 aclarado: scheduler es driving adapter local con `setTimeout`/`HH:MM`, no cron literal.
- **STAB-A7 (resuelto — documental):** 10 extensiones `x-idempotency` añadidas en `openapi.yaml` para operaciones admin/media; no cambia contrato de comunicación.
- **STAB-DRIFT-01 (resuelto por decisión del usuario Opción A, 2026-08-16 — documental):** `404 RESOURCE_NOT_FOUND` añadido a OpenAPI `provisionAdminUser` + aclaración Master Spec §ROP. Cambio de contrato de comunicación autorizado. **La corrección de código asociada STAB-B4 (operation-map.ts) permanece abierta como código pendiente.**
- **STAB-DRIFT-02 (resuelto por decisión del usuario Opción A, 2026-08-16 — documental):** descripción del `204` aclarada en OpenAPI de los webhooks + aclaración Master Spec §ROP de que `DUPLICATE_WEBHOOK_EVENT` 409 es clasificación interna no proyectada al proveedor. Sin cambio de statuses. No requiere corrección de código.

Hallazgos **abiertos** (no resueltos; se mantienen fuera de `## Resolved findings`):

- **Código pendiente (propiedad del Executor, no deuda):** STAB-B1 (ROP activate-admin), STAB-B2 (`must_change_password` replay `true`), STAB-B3 (JWT fail-fast del secreto), STAB-B4 (`operation-map.ts` 404), STAB-B5 (ROP JWT verify: `JwtPort.verify`→`Result`, `JwtAdapter.verify` traduce excepciones a `DomainError`, controller consume `Result` del puerto sin `try/catch`). No se ha tocado código; se enrutan a Executor solo tras veredicto `ready` + aprobación humana.
- **Decisión pendiente (no código, no deuda):** valores `iss` (issuer), `aud` (audience), `typ` y proveedor JWT (algoritmo/biblioteca) no fijados canónicamente. El estado actual del código (`jsonwebtoken`/HS256 sin `iss`/`aud`) es provisional y no constituye la decisión canónica. Debe definirse en una delta antes de producción (ver `## Open questions`). **STAB-B5 no cubre** `iss`/`aud`/`typ` ni el proveedor JWT; cubre solo la traducción ROP de `jwt.verify` (excepciones→`DomainError`/`Result`).
- **Deuda técnica activa (gates de producción / persistente, separada del código pendiente):** TD-MSF-ID-002-01 (scopes desconocidos `operation_pending`), TD-MSF-ID-002-02 (revisión legal/contable — gate antes de producción), TD-MSF-ID-002-03 (AWS scheduler coordinación/reemplazo — gate antes de producción si AWS participa), TD-MSF-API-001..007 (deuda persistente del API), TD-MSF-STAB-001/002/003 (se cierran con B3/B1/B2 respectivamente), TD-MSF-STAB-004 (nueva — ROP JWT verify, se cierra con B5).
- **Evidencia Graphify no verificable sin Git:** el contenido del grafo (1,245 nodos / 2,603 aristas, sin ciclos) está verificado en disco, pero la frescura respecto a `HEAD` no es verificable sin ejecutar Git (prohibido en esta sesión) → **blocked para frescura**. No se afirma que el grafo esté actualizado respecto a `HEAD`.

## Open questions

- STAB-DEC-01 Opción A vs Opción B: la decisión tomada es Opción A (compatible con Master Spec y shared context "no OpenAPI change"). Si el usuario prefiere reflejar el estado actual del recurso en el replay, se requiere delta OpenAPI explícita y revertir STAB-B2; marcar como `decision-required`.
- STAB-DRIFT-01 y STAB-DRIFT-02: **resueltos por decisión del usuario (Opción A, 2026-08-16)**; aplicados en disco. Ya no son `decision-required`; ver `Decisions locked` STAB-DEC-10/11.
- TD-MSF-ID-002-02 (legal/contable) y TD-MSF-ID-002-03 (AWS) permanecen como gates de producción; no se cierran en este incremento.
- **Decisión pendiente JWT (no fijada canónicamente):** los valores `iss` (issuer), `aud` (audience), `typ` y el proveedor JWT (algoritmo/biblioteca) no están definidos canónicamente. La evidencia en disco (`jwt.adapter.ts`) muestra una implementación provisional con `jsonwebtoken`/HS256, `expiresIn: '10m'`, sin `iss`/`aud` explícitos y `typ` por defecto de la librería; esto **no constituye la decisión canónica**. STAB-B3 cubre únicamente el fail-fast del secreto (`JWT_SECRET`); STAB-B5 cubre únicamente la traducción ROP de `jwt.verify` (excepciones→`DomainError`/`Result`). **Ninguno cubre** `iss`/`aud`/`typ` ni la elección del proveedor JWT. La decisión debe tomarse en una delta antes de producción; no se elige en este incremento.

## Stale terms guard

Forbidden: replay dinámico de `must_change_password` desde estado actual del recurso como regla vigente (Opción B rechazada); `try/catch` técnico en `application`; default de JWT en producción; ruta Prisma `apps/api/`; "1,136 nodos / 2,509 aristas" (cifras stale); "cron literal" como implementación del scheduler (es driving adapter local con `setTimeout`); "La migración 007 no implementa aún el job" (HISTÓRICO/SUPERSEDED por 007–013); "STAB-DRIFT-01/02 `decision-required`" (resueltos por Opción A del usuario 2026-08-16); "webhook devuelve 409 al proveedor por duplicado" (Opción B de STAB-DRIFT-02 rechazada: el webhook responde 204 idempotente); "eliminar el 404 del código de provision" (Opción B de STAB-DRIFT-01 rechazada); añadir 409 a los webhooks en OpenAPI; crear endpoints/migraciones/reglas de negocio; declarar `ready` sin Spec Validator; handoff sin aprobación humana; `POST /auth/initial-password-change`; `response` JSONB como regla vigente; PII/token/password/hash en `response_json`, logs o métricas; afirmar que el commit del grafo (`89bcd155`) es igual a `HEAD` o que el grafo está actualizado respecto a `HEAD` sin verificación con Git (blocked para frescura); afirmar que `iss`/`aud`/`typ` o el proveedor JWT (algoritmo/biblioteca) están fijados canónicamente como decisión vigente (son decisión pendiente; el estado actual del código `jsonwebtoken`/HS256 sin `iss`/`aud` es provisional y no constituye la decisión canónica); afirmar que STAB-B5 cubre `iss`/`aud`/`typ` o el proveedor JWT (B5 cubre solo la traducción ROP de `jwt.verify`; `iss`/`aud`/`typ`/proveedor siguen como decisión pendiente STAB-DEC-12); afirmar que STAB-B5 cubre el fail-fast del secreto `JWT_SECRET` (eso es B3); afirmar que STAB-B5 cubre `jwt.sign` o el `try/catch` de los use cases `register`/`login`/`refresh-session` (B5 es solo `jwt.verify`; el `try/catch` de los use cases sobre `sign`/otros adapters es hallazgo separado, no B5); "JwtAdapter propaga excepciones de `jwt.verify`" como patrón vigente (debe traducir a `DomainError`); "JwtPort.verify devuelve `Promise<JwtPayload>`" como patrón vigente (debe devolver `Result<JwtPayload, DomainError>`); "controller inyecta `JwtAdapter` concreto" como patrón vigente (debe inyectar `JwtPort`); "controller captura excepciones de `jwt.verify` con `try/catch`" como patrón vigente (debe consumir `Result` del puerto); inventar códigos nuevos del catálogo `DomainError` para B5 (se reutilizan `AUTHENTICATION_REQUIRED` 401 y `TECHNICAL_DEPENDENCY_FAILURE` 500 del catálogo existente ADR-017); confundir código pendiente (B1-B5) con deuda técnica (TD-MSF-*) o con evidencia Graphify no verificable sin Git. Nota: el cambio OpenAPI de contrato de comunicación (añadir 404 a `provisionAdminUser`, STAB-DRIFT-01 Opción A) fue autorizado explícitamente por el usuario; la restricción general de no cambiar OpenAPI de contrato de comunicación sin autorización se mantiene para futuros cambios.

## Next action

Cierre y consolidación del incremento de estabilización. B1-B5 implementadas y verificadas en disco (2026-08-16); TD-MSF-STAB-001/002/003/004 marcadas `resolved-by-verified-implementation` en `technical_debt.md` (global y espejo local) y `workspace_changes.md`. Pasos de cierre/consolidación:
1. Consolidar las decisiones durables del incremento de estabilización en la Master Spec (documentation-lifecycle) y archivar artefactos temporales obsoletos como `superseded` si aplica.
2. Los gates de producción TD-MSF-ID-002-02 (legal/contable) y TD-MSF-ID-002-03 (AWS) permanecen abiertos; no se declara listo para producción.
3. La decisión JWT (`iss`/`aud`/`typ` y proveedor) sigue pendiente para una delta futura (STAB-DEC-12); no se elige en este incremento.
4. El `verdict: ready` de Spec Validator (2026-08-16) y la aprobación humana (`## Human Plan Approval: approved_by_user`) permanecen como trazabilidad; no se invalidan por esta consolidación documental de cierre.
5. **Clasificación de estados (no mezclar):** (a) **código implementado y verificado** = STAB-B1/B2/B3/B4/B5, verificados en disco 2026-08-16; (b) **deuda técnica resuelta** = TD-MSF-STAB-001/002/003/004, `resolved-by-verified-implementation`; (c) **deuda técnica activa (gates de producción)** = TD-MSF-ID-002-02/03, permanecen abiertos; (d) **deuda técnica persistente** = TD-MSF-ID-002-01, TD-MSF-API-001..007, se resuelven con sus tareas respectivas; (e) **decisión pendiente** = `iss`/`aud`/`typ` y proveedor JWT, no fijados canónicamente (STAB-DEC-12); (f) **evidencia Graphify** = contenido verificado en disco (1,245 nodos / 2,603 aristas, sin ciclos) pero frescura respecto a `HEAD` **blocked para frescura** sin Git.
