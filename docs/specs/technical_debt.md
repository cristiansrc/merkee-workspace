# Registro global de deuda técnica

**Lifecycle status:** `revision-needed`  
**Actualizado:** 2026-08-21  
**Registro espejo local:** `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/projects/merkee-shop-api/docs/specs/technical_debt.md`  
**Nota de frescura 2026-08-21:** este registro no resuelve deudas sin evidencia. Lo verificado postentrega (ECS estable, ECR publicado, CORS allowlist + PUT, `images.merkee.shop` OAC, `register` cliente `must_change=false`, cart guest→cliente transfer, sesión 30m) se documenta en `README.md` y `docs/DEPLOYMENT_STATUS.md` con commits verificados (`7fdb009`/`215b36b`/`932a71a`/`91ed871`/`02167cd`/`8948426`/`fe0b121`). **No se declara producción lista** — gates `TD-AWS-RDS-PUBLIC`, `TD-AWS-OBSERVABILITY`, `TD-MSF-ID-002-02/03` siguen `active`.

**Alcance de la actualización 2026-08-17 (saneamiento del registro):** se editaron **únicamente** los dos registros de deuda técnica (este global y su espejo local). No se tocó código, Git, scripts, OpenAPI, Prisma/migraciones, `package.json`, runtime ni otras specs. La tabla `Deuda activa` queda restringida a la deuda técnica real vigente (scopes desconocidos, revisión legal/contable, coordinación AWS). Nada se declara resuelto sin evidencia: los pendientes verificados que salen de esa tabla se conservan explícitamente como **abiertos** en `Pendientes verificados`, y solo el claim factualmente invalidado por el código vigente pasa a `HISTÓRICO/SUPERSEDED`.

**Alcance de la actualización 2026-08-17 (sincronización documental MSF-ID-003):** pasada documental posterior a la implementación del password reset y la migración 014. Se editaron: este registro global, su espejo local, `docs/specs/tasks/merkee-shop-foundation-task-board.md`, `docs/specs/master_spec.md`, `docs/specs/prisma-migration-contract.md`, `docs/specs/architecture-decisions.md` y `docs/specs/.working/merkee-shop-foundation-sdd-context.md`. No se tocó código, Git, scripts, OpenAPI, Prisma/migraciones, `package.json` ni runtime. TD-MSF-API-003 pasa a `HISTÓRICO/SUPERSEDED` (handlers implementados); se registra TD-MSF-ID-003-01 (riesgo de preflight PostgreSQL para la migración 014) y TD-MSF-ID-003-C01 (implementación verificada). El `ready` histórico de Spec Validator queda invalidado; la revalidación queda pendiente.

**Alcance de la actualización 2026-08-18 (reconciliación con estado real en disco):** se corrigieron las entradas TD-MSF-API-005 (storefront/admin ya tienen código, dependencias y build → `HISTÓRICO/SUPERSEDED`), TD-NEW-COV (`coverageThreshold` sí está definido en `package.json` → `HISTÓRICO/SUPERSEDED`) y TD-MSF-API-004 (el script `test:integration` sí está definido en `package.json`; se corrigió la evidencia). No se tocó código, Git, scripts, OpenAPI, Prisma/migraciones ni runtime.

**Alcance de la actualización 2026-08-18 (avances de infraestructura AWS verificados):** se añaden las entradas de deuda TD-AWS-RDS-PUBLIC, TD-AWS-SWAGGER-DNS, TD-AWS-OBSERVABILITY y TD-AWS-ECS-VALIDATION, y una nota de estado AWS parcial en la tabla `Deuda activa`. No se tocó código, Git, scripts, OpenAPI, Prisma/migraciones, `package.json` ni runtime. No se afirma despliegue productivo terminado; el servicio ECS permanece en despliegue/pendiente de verificación.

**Alcance de la actualización 2026-08-21 (rehidratación honesta postentrega, sin cierre de gates):** se rehidrata la frescura del registro a 2026-08-21 sin resolver deuda sin evidencia. No se tocó código, Git, scripts, OpenAPI, Prisma/migraciones, `package.json`, runtime ni secretos AWS. Lo verificado postentrega (ECS estable, ECR publicado, CORS allowlist + PUT `7fdb009`/`932a71a`, media real `images.merkee.shop` OAC `91ed871`/`02167cd`, `register` cliente `must_change=false` `f62cee4`, cart guest anonymous + transfer `9df9187`/`8948426`/`fe0b121`, sesión 30m `580ff8f`, soft-delete operativa, imágenes resueltas, banners toggle) se mantiene en `README.md` y `docs/DEPLOYMENT_STATUS.md` como evidencia fechada. **Gates abiertos siguen `active`:** `TD-AWS-RDS-PUBLIC`, `TD-AWS-SWAGGER-DNS`, `TD-AWS-OBSERVABILITY`, `TD-MSF-ID-002-02/03`; no se declara producción lista.

## Deuda activa — sincronizada con `merkee-shop-api`

| ID | Descripción e impacto | Responsable | Condición de cierre | Evidencia requerida para cierre | Estado |
|---|---|---|---|---|---|
| TD-MSF-ID-002-01 | Los scopes desconocidos de `idempotency_records` se conservan como `operation_pending` y no se purgan. Esto puede incrementar el almacenamiento hasta que su operación tenga terminalidad definida. | Planner + Executor de la mutación futura | Para cada mutación futura que use `idempotency_records`, una delta aprobada define su scope, criterio de terminalidad, retención y prueba de purga. Los scopes ya desconocidos no se reclasifican ni purgan por esta entrada. | Delta de la mutación, contrato de scope/terminalidad/retención y prueba que demuestre `operation_pending` antes de la terminalidad y purga solo después de cumplirla. | active — no bloqueante para desarrollo local; no se cierra globalmente por una sola mutación. |
| TD-MSF-ID-002-02 | La retención y anonimización son una política técnica provisional; falta revisión legal/contable aplicable antes de producción. | Product/Legal | Existe una decisión legal/contable explícita que aprueba o corrige la política de retención y anonimización aplicable a producción, y la corrección documental/contractual resultante está aprobada. | Documento de revisión legal/contable fechado, responsable identificable, alcance normativo y decisión sobre retención/anonimización; si cambia la política, delta y validación de los contratos afectados. | active — no bloqueante para desarrollo local; **gate antes de producción**. |
| TD-MSF-ID-002-03 | AWS no está configurado. El scheduler local de purga y las métricas productivas ya están implementados. Falta decidir si AWS coordina o reemplaza el scheduler, su ownership, alarmas y la prevención verificable de doble ejecución. | Plataforma/Operaciones | Una decisión operativa aprobada define si AWS coordina o reemplaza el scheduler local, ownership, alarmas y mecanismo de exclusión/prevención de doble ejecución; no se habilita AWS antes de esa definición. | Diseño operativo/infraestructura aprobado, evidencia de ownership y alarmas, y prueba o evidencia operacional de que scheduler local y AWS no ejecutan duplicadamente el mismo ciclo. | active — no bloqueante para desarrollo local; **gate antes de producción** si se despliega u opera con AWS. |
| TD-AWS-RDS-PUBLIC | RDS `merkee-db` expuesta a internet (`PubliclyAccessible=True` según auditoría 2026-08-18). Riesgo de seguridad antes de producción. | Plataforma/Seguridad | `merkee-db` con `PubliclyAccessible=False` y conectividad verificada desde ECS vía VPC/SG. | Evidencia de `describe-db-instances` con `PubliclyAccessible=False` y prueba de conexión desde el task role. | active — **gate antes de producción**; no se afirma corrección. |
| TD-AWS-SWAGGER-DNS | `swagger.merkee.shop` (documentación OpenAPI) pendiente de distribución CloudFront/origen. | Plataforma/Operaciones | Distribución CloudFront con origen al servicio/documentación y registro DNS en Spaceship. | Evidencia de distribución desplegada y resolución DNS del subdominio. | active — no bloqueante para desarrollo local; recomendado antes de producción. |
| TD-AWS-OBSERVABILITY | CloudWatch Alarms no configuradas para la API (5xx, latencia, CPU/memoria). | Plataforma/Operaciones | Alarmas CloudWatch configuradas con umbrales acordados. | Evidencia de alarms creadas y suscritas a métricas del servicio ECS. | active — no bloqueante para desarrollo local; recomendado antes de producción. |
| TD-AWS-ECS-VALIDATION | Servicio ECS `merkee-backend-service` en despliegue; running y health check pendientes de verificación; imagen por confirmar. No se afirma despliegue terminado. | Plataforma/Operaciones | Servicio con tasks running estables, health check OK y logs en CloudWatch. | Evidencia de `describe-services` con running=desired, health check OK y logs sin error de arranque. | active — **gate antes de declarar despliegue**; no se afirma éxito. |
| TD-ADMIN-AUTH-001 | `TransportAuthGuard` solo verifica presencia sintáctica del header `Authorization: Bearer <token>` (regex), no invoca `JwtPort.verify`, no asigna `req.user`. `cookie-parser` no está en `package.json`/`main.ts`, por lo que `req.cookies` queda `undefined` en runtime (los tests del controller usan mocks que sí lo populan). `POST /v1/auth/logout` no emite `Set-Cookie` con `Max-Age=0` para limpiar `merkee_refresh_session`. Resultado: `POST /v1/auth/refresh` siempre devuelve 401 en condiciones reales; `GET /me`/`PATCH /me`/`logout`/`password-change` reciben `actor=null`; el admin no puede operar sobre la API real aunque el login devuelva 200. | Planner + Executor del incremento `msf-admin-real-auth-and-cookie-guard` (`planning`) | Implementación verificada de DEC-01 (cookie-parser middleware), DEC-02 (TransportAuthGuard reescrito con `JwtPort.verify` y `req.user`), DEC-03 (`logout` emite `clearCookie` con `Max-Age=0` en éxito), DEC-04 (tests actualizados) y DEC-06 (DI correcta). Sin cambios en OpenAPI, Prisma, migraciones, DTOs ni claims JWT. | Diff de `main.ts`, `package.json`, `transport-auth.guard.ts`, `identity.controller.ts`, `transport-auth.guard.spec.ts` y `identity.controller.spec.ts` consistente con §6.2 de la delta spec. `npm run build` OK; `npm test` PASS (≥5 casos en `TransportAuthGuard`, 2 nuevos en `IdentityController.logout`); `npm run depcruise` sin violaciones (guard y controller inyectan `JwtPort`, no `JwtAdapter`). Pruebas E2E curl con cookie-jar documentadas en §4.3 de la delta spec. | active — no bloqueante para desarrollo local; se resuelve con la implementación del incremento `msf-admin-real-auth-and-cookie-guard` (planning). |

Los responsables, condiciones de cierre y evidencia requerida son canónicos e idénticos en este registro y en el espejo local.

**Nota AWS (2026-08-18 histórica; rehidratada 2026-08-21):** AWS está configurado en cuenta de aprendizaje (us-east-1, un ambiente): ECS task definition `merkee-backend-task` (histórico revision 2 → verificado estable 2026-08-21), `taskRole` y mapeo `secrets` JSON, secreto `merkee/app` en Secrets Manager, buckets/CloudFront storefront (`E32P11SX9DFU82`→`merkee.shop`) y admin (`E119IKP00L5RU`→`admin.merkee.shop`) **+ media `images.merkee.shop` OAC** (`91ed871`/`02167cd`), IAM OIDC `merkee-github-actions-deploy`, CORS allowlist + PUT (`7fdb009`/`932a71a`), prefijo `/v1` (`215b36b`), `cookie-parser` + `JwtPort.verify` + `clearCookie` (`932a71a`/`9e3ad3e`), cart guest transfer (`8948426`/`fe0b121`). Esto **no cierra** TD-MSF-ID-002-03 (falta decisión de coordinación/reemplazo del scheduler, ownership, alarms y prevención de doble ejecución). Las entradas TD-AWS-* registran riesgos específicos del despliegue pendientes de verificación (TD-AWS-RDS-PUBLIC, TD-AWS-SWAGGER-DNS, TD-AWS-OBSERVABILITY siguen `active` — **gates abiertos**, no se declara producción lista).

## Claims obsoletos — HISTÓRICO/SUPERSEDED por el código vigente

| ID | Claim retirado | Evidencia que lo invalida (verificada en disco 2026-08-17) | Estado |
|---|---|---|---|
| TD-NEW-ROP-PURGE | Que `purge-idempotency-records.use-case.ts` `execute()` devuelve `Promise<void>` y que contiene `try/catch` técnico/operacional en `application` (supuestas líneas 52-80 y 90-101), con la consiguiente afirmación de drift de ADR-017. | `projects/merkee-shop-api/src/modules/identity/application/use-cases/purge-idempotency-records.use-case.ts` (212 líneas): L60 `async execute(): Promise<Result<void, DomainError>>`; L6 importa `Result, ok, fail, isFailure`; L76-78 propaga el `Failure` del adapter con `isFailure` sin capturar; L93 `return ok(undefined)` en éxito; L103-130 `processBatchWithRetries` implementa los reintentos 1/5/15 s (`BATCH_RETRY_DELAYS_MS`, `MAX_BATCH_ATTEMPTS=3`) sobre el rail `Result` con `isFailure`, sin `try/catch`; L183-193 propaga el `Failure` de `repo.purgeBatch`. Grep de `try {`/`catch (` en `src/modules/identity/application/use-cases` (2026-08-17): **cero ocurrencias en este archivo**. El driving adapter `infrastructure/adapters/scheduled-idempotency-purge.adapter.ts` L100-115 consume el `Result` (`isFailure` L102), registra `recordRun('error')`/`recordError()` L104-105 y log sanitizado `idempotency_records.purge_failed` solo con `code` L110-112, sin relanzar. Traza documental concordante: `docs/specs/tasks/merkee-shop-foundation-task-board.md` L11 y L202 (nota `idempotency_responsejson_rop_purge_note`, 2026-08-16): "`execute()` ahora devuelve `Promise<Result<void, DomainError>>` y el `try/catch` técnico se eliminó de `application`". | `HISTÓRICO/SUPERSEDED` — claim factualmente incorrecto respecto al código vigente; retirado del registro de deuda. No es una deuda resuelta por trabajo pendiente: el patrón ROP ya está implementado y verificado. |
| TD-MSF-API-005 | Que `projects/merkee-shop-admin` y `projects/merkee-shop-storefront` "contienen únicamente README.md y .gitignore; sin código, build ni dependencias". | Verificación en disco 2026-08-18: ambos proyectos contienen `package.json`, `node_modules/`, `dist/`, `src/` con código TypeScript, `vite.config.ts`, `vitest.config.ts` y `.oxlintrc.json` (admin); dependencias instaladas y build generado. | `HISTÓRICO/SUPERSEDED` — claim factualmente incorrecto respecto al estado real en disco; retirado del registro de deuda. No es deuda resuelta por trabajo: el scaffolding ya existía. |
| TD-NEW-COV | Que `package.json` "no define `coverageThreshold`; no hay gate de cobertura". | `package.json` (2026-08-18) define `coverageThreshold` (gate de cobertura por global/dominio/aplicación). El gate de cobertura sí existe. | `HISTÓRICO/SUPERSEDED` — claim factualmente incorrecto respecto a `package.json` vigente; retirado del registro de deuda. La *medición* de cobertura sigue pendiente (no se afirma porcentaje concreto), pero el gate configurado existe. |
| TD-MSF-API-002 | Que los módulos `catalog`, `cart-reservation`, `checkout`, `media`, `orders`, `payments`, `admin-query` están en "estructura hexagonal y lógica placeholder del scaffolding MSF-API-001". | Verificación en disco 2026-08-18: cada módulo contiene `application/use-cases/*.ts` con implementación ROP y `*.spec.ts` (p. ej. catalog: admin-banner/category/product/stock-adjustment, get/list; media: create-upload-url; cart-reservation: add/get/remove/set/transition/expire; orders: list; payments: create/webhook/reconcile; checkout: create). | `HISTÓRICO/SUPERSEDED` — claim factualmente incorrecto respecto al estado real en disco; retirado del registro de deuda. El trabajo restante (integración externa S3/pagos/email y hardening HTTP) está cubierto por TD-MSF-ID-002-03, TD-MSF-API-001 y TD-NEW-HTTP-SEC. |

**Claims residuales fuera de estos registros (no editados hoy, fuera del alcance autorizado):** `docs/specs/increments/msf-id-rop-sign-cleanup-delta-spec.md` (L25, L89-99, L271, L277, L289, L304, L324, L342) y `docs/specs/.working/msf-id-rop-sign-cleanup-sdd-context.md` (L14, L23, L63, L83, L101) siguen afirmando el claim retirado (`purge` devuelve `void` / `try/catch` operacional) y registran `TD-NEW-ROP-PURGE` como entrada a crear. Requieren corrección documental en su propio incremento antes de cualquier veredicto de Spec Validator; este registro ya no respalda esas afirmaciones.

## Pendientes verificados — fuera del registro de deuda activa, NO resueltos

Estas entradas salen de la tabla `Deuda activa` por reclasificación editorial (trabajo pendiente planificado, gates de proceso o correcciones ya asignadas a un incremento), **no por resolución**. La verificación de disco del 2026-08-17 confirma que siguen abiertas; su ownership y su condición de cierre permanecen en el task board y en las deltas correspondientes. Prohibido citarlas como resueltas.

| ID | Hecho verificado en disco (2026-08-17) | Dueño | Estado |
|---|---|---|---|
| TD-MSF-API-001 | `src/modules/identity/infrastructure/adapters/noop-cart-reservation.adapter.ts` existe: el `CartReservationPort` de identity sigue siendo no-op, por lo que logout y login guest→admin no liberan reservas reales. | Executor (MSF-CART-001) | abierto — pendiente de implementación; **gate antes de producción** (no-op en flujo productivo). |
| TD-MSF-API-002 | *Movido a `HISTÓRICO/SUPERSEDED` (sección correspondiente): verificado en disco 2026-08-18 que `catalog` (incl. stock), `media`, `cart-reservation`, `orders`, `payments`, `checkout` y `admin-query` tienen casos de uso ROP + adapters + pruebas (no son placeholder).* | — | `HISTÓRICO/SUPERSEDED` (claim invalidado). |
| TD-MSF-API-003 | `/auth/password-reset-requests`, `/auth/password-resets` y `/auth/password-change` solo aparecen en `src/contract/operation-map.ts` (L124, L136, L148); no hay handlers `GET/PATCH /me` ni de cambio/recuperación de contraseña en `src/`. | Executor (MSF-ID-003) | abierto — pendiente de implementación (MSF-ID-003 `blocked`). **HISTÓRICO/SUPERSEDED 2026-08-17:** MSF-ID-003 implementado; los handlers existen en `identity.controller.ts` (`GET /me`, `PATCH /me`, `POST /auth/password-change`, `POST /auth/password-reset-requests`, `POST /auth/password-resets`). Ver `Cambios completados` TD-MSF-ID-003-C01. |
| TD-MSF-API-004 | `scripts/integration-msf-id-002.ts` y `scripts/integration-purge-msf-id-002.ts` existen y `package.json` define `test:integration` (L15, `jest --config jest.integration.config.js --runInBand`); sin embargo no corre en la suite por defecto (`npm test`) ni está verificado en CI. | Test-Architect (MSF-TEST-001) | abierto — recomendado antes de producción; cablear en CI pendiente de confirmación. |
| TD-MSF-API-005 | *Movido a `HISTÓRICO/SUPERSEDED` (sección correspondiente): verificado en disco 2026-08-18 que storefront/admin tienen código, `package.json`, `node_modules` y `dist`.* | — | `HISTÓRICO/SUPERSEDED` (claim invalidado). |
| TD-MSF-API-006 | `docs/specs/tasks/merkee-shop-foundation-task-board.md` L5 `Estado: blocked`, L11 `Spec validation: pending / revision-needed`, L14 confirma que todas las tareas no ejecutadas siguen `blocked`; `docs/specs/workspace_changes.md` L5 `Spec Validator: pending`. | Spec-Validator + Planner | abierto — gate de proceso, no deuda técnica persistente. |
| TD-MSF-API-007 | El gate de revalidación documental sigue abierto (misma evidencia que TD-MSF-API-006); la consistencia código↔documentación no ha sido confirmada por Spec Validator tras las correcciones iterativas de MSF-ID-002. Nota: el drift de purga que se citaba como ejemplo ya no aplica (ver `HISTÓRICO/SUPERSEDED`). | Spec-Validator + Reviewer | abierto — gate de proceso; se resuelve junto con TD-MSF-API-006. |
| TD-MSF-ID-003-01 | La migración `014_password_reset_tokens_active_unique_index` crea un índice parcial único `WHERE used_at IS NULL` por `user_id`; si la base contiene tokens de reset activos duplicados preexistentes (tabla creada en 001), la migración fallará con violación de unicidad y bloqueará `prisma migrate deploy`. Requiere preflight PostgreSQL (`SELECT user_id, count(*) FROM password_reset_tokens WHERE used_at IS NULL GROUP BY user_id HAVING count(*) > 1`) y resolución de duplicados antes de aplicar en cualquier entorno con datos. | Plataforma/Operaciones + Executor (MSF-ID-003) | abierto — riesgo operativo de despliegue; no bloquea desarrollo local; **gate antes de aplicar 014 en entornos con datos**. |
| TD-NEW-ROP-SIGN | `try {` presente en `application`: `logout.use-case.ts` L34, `refresh-session.use-case.ts` L48, `login.use-case.ts` L55, `register.use-case.ts` L48. El drift ROP de estos cuatro use cases sigue vigente y sigue asignado al incremento `msf-id-rop-sign-cleanup` (`planning`). | Planner + Executor (`msf-id-rop-sign-cleanup`) | abierto — deuda técnica real en su incremento; no bloqueante para desarrollo local. |
| TD-NEW-HTTP-SEC | `src/main.ts` tiene 10 líneas y solo hace `NestFactory.create` + `app.listen`: sin helmet/CSP/HSTS/nosniff, sin CORS allowlist, sin CSRF (Origin/double-submit) y sin rate limiting de login/registro/reset/activación exigidos por Master Spec §Identidad. | Plataforma/Seguridad + Executor (incremento de seguridad HTTP o MSF-OPS-001) | abierto — **gate antes de producción**; no desplegar sin las protecciones HTTP exigidas. |
| TD-NEW-COV | *Movido a `HISTÓRICO/SUPERSEDED` (sección correspondiente): `package.json` sí define `coverageThreshold`.* | — | `HISTÓRICO/SUPERSEDED` (claim invalidado). |

## Cambios completados — no son deuda activa

| ID | Evidencia | Estado |
|---|---|---|
| TD-MSF-ID-002-C01 | La secuencia aplicada `007_idempotency_records`, `008_idempotency_records_purge_index`, `009_idempotency_records_response_json_rename`, `010_idempotency_records_response_json_backfill`, `011_idempotency_records_response_json_normalize`, `012_idempotency_records_response_json_validate` y `013_idempotency_records_response_json_strict_validate` deja `response_json` con snapshot mínimo y validación estricta. | resolved-by-verified-implementation |
| TD-MSF-ID-002-C02 | El replay se reconstruye desde el recurso actual; no persiste respuesta completa ni PII. | resolved-by-verified-implementation |
| TD-MSF-ID-002-C03 | Scheduler local y bridge de métricas productivas están implementados; el adapter in-memory es solo de pruebas. Esto no resuelve la decisión operativa AWS de TD-MSF-ID-002-03. | resolved-by-verified-implementation |
| TD-MSF-ID-002-C04 | El bootstrap valida `role=admin` antes del no-op. | resolved-by-verified-implementation |
| TD-MSF-STAB-001 | JWT fail-fast verificado en disco (2026-08-16): `jwt.adapter.ts` lanza error de arranque en `NODE_ENV=production` si `JWT_SECRET` ausente o < 32 bytes; default solo en desarrollo con advertencia; `.env.example` documenta `JWT_SECRET` (mín. 32 bytes, no versionar valor real). Corrección STAB-B3 aplicada. | resolved-by-verified-implementation |
| TD-MSF-STAB-002 | ROP activate-admin verificado en disco (2026-08-16): `ActivateAdminUnitOfWorkPort.run` devuelve `Result<T, DomainError>`; `PrismaActivateAdminUnitOfWorkAdapter` captura y traduce a `fail(technicalFailure())` sin causa/PII; `activate-admin.use-case.ts` sin `try/catch` técnico. Corrección STAB-B1 aplicada. | resolved-by-verified-implementation |
| TD-MSF-STAB-003 | `must_change_password` en replay verificado en disco (2026-08-16): `provision-admin-user.use-case.ts` fuerza `must_change_password: true` en `replayFromResource`; `schemas.ts` tipa `must_change_password: true` (literal); OpenAPI `const: true` sin cambio. Corrección STAB-B2 aplicada (STAB-DEC-01 Opción A). | resolved-by-verified-implementation |
| TD-MSF-STAB-004 | ROP JWT verify verificado en disco (2026-08-16): `JwtPort.verify` devuelve `Result<JwtPayload, DomainError>`; `JwtAdapter.verify` captura `TokenExpiredError`/`JsonWebTokenError`/`NotBeforeError` y traduce a `DomainError` del catálogo existente (`AUTHENTICATION_REQUIRED` 401 / `TECHNICAL_DEPENDENCY_FAILURE` 500) sin causa/PII; el controller `extractSessionId`/`extractUserId` consume `Result` del puerto `JwtPort` (no del adapter concreto) sin `try/catch`; DI en `identity.module.ts` actualizada. Corrección STAB-B5 aplicada. No cubre `iss`/`aud`/`typ` ni el proveedor JWT (decisión pendiente STAB-DEC-12). | resolved-by-verified-implementation |
| TD-NEW-STAB-LIFECYCLE | Lifecycle del incremento `merkee-shop-foundation-stabilization` alineado a `closed` con evidencia (2026-08-16): task board de estabilización `done` (`docs/specs/tasks/merkee-shop-foundation-stabilization-task-board.md` línea 5); B1-B5 (ROP activate-admin, `must_change_password` replay `true`, JWT fail-fast del secreto, `operation-map.ts` 404, ROP JWT verify) implementados y verificados en disco; TD-MSF-STAB-001/002/003/004 `resolved-by-verified-implementation`; delta spec y shared context de estabilización marcados `closed` por SCC-A1. | resolved-by-verified-implementation |
| TD-MSF-ID-003-C01 | MSF-ID-003 implementado (2026-08-17): handlers `GET /me`, `PATCH /me`, `POST /auth/password-change`, `POST /auth/password-reset-requests` (202 neutro) y `POST /auth/password-resets` (204) en `identity.controller.ts`; use cases ROP `request-password-reset` y `reset-password`; UoW atómicos `RequestPasswordResetUnitOfWorkPort`/`ResetPasswordUnitOfWorkPort` (SERIALIZABLE, rollback total); `NoopEmailAdapter` sin PII/token en logs; migración `014_password_reset_tokens_active_unique_index` (índice parcial único `WHERE used_at IS NULL` por `user_id`, sin editar 001–013). Evidencia: 42 suites/297 tests PASS, build OK, depcruise 185 modules/644 dependencies, `prisma validate` OK (task board `blockers_correction_note`). No modifica OpenAPI. **No cierra la revalidación de Spec Validator ni los gates TD-MSF-ID-002-02/03.** | resolved-by-verified-implementation |
