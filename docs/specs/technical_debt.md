# Registro global de deuda técnica

**Lifecycle status:** `revision-needed`  
**Actualizado:** 2026-08-16  
**Registro espejo local:** `/home/cristiansrc/Documentos/Proyectos/merkee-workspace/projects/merkee-shop-api/docs/specs/technical_debt.md`

## Deuda activa — sincronizada con `merkee-shop-api`

| ID | Descripción e impacto | Responsable | Condición de cierre | Evidencia requerida para cierre | Estado |
|---|---|---|---|---|---|
| TD-MSF-ID-002-01 | Los scopes desconocidos de `idempotency_records` se conservan como `operation_pending` y no se purgan. Esto puede incrementar el almacenamiento hasta que su operación tenga terminalidad definida. | Planner + Executor de la mutación futura | Para cada mutación futura que use `idempotency_records`, una delta aprobada define su scope, criterio de terminalidad, retención y prueba de purga. Los scopes ya desconocidos no se reclasifican ni purgan por esta entrada. | Delta de la mutación, contrato de scope/terminalidad/retención y prueba que demuestre `operation_pending` antes de la terminalidad y purga solo después de cumplirla. | active — no bloqueante para desarrollo local; no se cierra globalmente por una sola mutación. |
| TD-MSF-ID-002-02 | La retención y anonimización son una política técnica provisional; falta revisión legal/contable aplicable antes de producción. | Product/Legal | Existe una decisión legal/contable explícita que aprueba o corrige la política de retención y anonimización aplicable a producción, y la corrección documental/contractual resultante está aprobada. | Documento de revisión legal/contable fechado, responsable identificable, alcance normativo y decisión sobre retención/anonimización; si cambia la política, delta y validación de los contratos afectados. | active — no bloqueante para desarrollo local; **gate antes de producción**. |
| TD-MSF-ID-002-03 | AWS no está configurado. El scheduler local de purga y las métricas productivas ya están implementados. Falta decidir si AWS coordina o reemplaza el scheduler, su ownership, alarmas y la prevención verificable de doble ejecución. | Plataforma/Operaciones | Una decisión operativa aprobada define si AWS coordina o reemplaza el scheduler local, ownership, alarmas y mecanismo de exclusión/prevención de doble ejecución; no se habilita AWS antes de esa definición. | Diseño operativo/infraestructura aprobado, evidencia de ownership y alarmas, y prueba o evidencia operacional de que scheduler local y AWS no ejecutan duplicadamente el mismo ciclo. | active — no bloqueante para desarrollo local; **gate antes de producción** si se despliega u opera con AWS. |
| TD-MSF-API-001 | `NoopCartReservationAdapter` en producción. El adapter de `CartReservationPort` en identity (`src/modules/identity/infrastructure/adapters/noop-cart-reservation.adapter.ts`) es un no-op. Login guest→admin y logout no liberan reservas reales hasta que MSF-CART-001 implemente el adapter real. | Executor (MSF-CART-001) | MSF-CART-001 implementa el `CartReservationPort` real y reemplaza el no-op en `identity.module.ts`. | Test que demuestre que logout libera reservas ACTIVE y login guest→admin libera ACTIVE + cierra carrito contra PostgreSQL real. | active — no bloqueante para desarrollo local; se resuelve al completar MSF-CART-001. |
| TD-MSF-API-002 | Módulos no-identity son skeletons. `catalog`, `cart-reservation`, `checkout`, `media`, `orders`, `payments` y `admin-query` tienen estructura hexagonal (controllers, use cases, ports, adapters) pero su lógica es placeholder del scaffolding MSF-API-001. No implementan reglas de negocio reales. | Executor (tareas respectivas) | Cada módulo completa su implementación según su tarea en el task board (MSF-CAT-001/002/003, MSF-CART-001/002, MSF-PAY-001/002/003/004, MSF-MEDIA-001). | Tests unitarios y de integración por módulo con cobertura ≥85%. | active — no bloqueante; es el estado esperado del desarrollo incremental. |
| TD-MSF-API-003 | MSF-ID-003 bloqueado. Perfil de usuario (`GET/PATCH /me`), cambio de contraseña (`POST /v1/auth/password-change`) y recuperación (`POST /v1/auth/password-reset-requests`, `POST /v1/auth/password-resets`) no están implementados. | Executor (MSF-ID-003) | MSF-ID-003 completado con todos los endpoints funcionales, tests ROP, idempotencia y revocación de sesiones. | 34+ suites de tests adicionales, endpoints funcionales contra PostgreSQL, tests de idempotencia concurrente. | active — dependencia de MSF-STORE-003 y MSF-ADMIN-001; no bloquea otras tareas del API. |
| TD-MSF-API-004 | Pruebas de integración real fuera de la suite principal. `scripts/integration-msf-id-002.ts` y `scripts/integration-purge-msf-id-002.ts` validan contra PostgreSQL real pero no son parte de `npm test`. No se ejecutan en CI. | Test-Architect (MSF-TEST-001) | Los scripts de integración se integran en la suite de tests (Jest) o se configura un comando `npm run test:integration` que se ejecute en CI. | Comando documentado y funcional que ejecuta las pruebas de integración contra PostgreSQL (Testcontainers o Docker). | active — no bloqueante para desarrollo local; recomendado antes de producción. |
| TD-MSF-API-005 | Proyectos admin y storefront sin scaffolding. `merkee-shop-admin` y `merkee-shop-storefront` solo contienen README.md y .gitignore. No tienen código fuente, configuración de build ni dependencias. | Executor (MSF-STORE-001, MSF-ADMIN-001) | MSF-STORE-001 y MSF-ADMIN-001 completados con shell React/Redux funcional. | Build exitoso de cada SPA, cliente API generado desde OpenAPI, tests de guards/store. | active — dependencia de todas las tareas de frontend (MSF-STORE-002/003, MSF-ADMIN-002/003); no bloquea tareas del API. |
| TD-MSF-API-006 | Task board bloqueado por Spec Validator pendiente. 21 de 29 tareas en estado `blocked` (8 `done`: MSF-API-001/002/003, MSF-DATA-001/002/003, MSF-ID-001/002) porque la documentación no ha sido revalidada tras las correcciones de MSF-ID-002 y la remediación documental de estabilización. | Spec-Validator + Planner | Spec Validator emite veredicto `ready` sobre el conjunto canónico (Master Spec, Prisma contract 007-013, ADR-018, SDD context, task board) más la delta de estabilización. | Veredicto `ready` del Spec Validator registrado en SDD context. | active — gate temporal de validación documental; no es deuda persistente. Se resuelve con veredicto ready del Spec Validator. |
| TD-MSF-API-007 | Posible drift entre documentación y código. Las 12 correcciones iterativas a MSF-ID-002 (privacy_replay, purge, bootstrap_role, metrics_scheduler, response_json_replay, purge_blockers, bootstrap_transaction, snapshot_idempotency_blockers, three_findings_fix, rop_migration_validate, exclusive_classification, bootstrap_rop_migration013) modificaron código y specs sin revalidación posterior. No hay garantía de que el código coincida exactamente con master_spec, ADR-018 y prisma-migration-contract. | Spec-Validator + Reviewer | Spec Validator confirma consistencia entre documentación canónica y código implementado. Reviewer verifica que no hay drift en identity module. | Veredicto `ready` del Spec Validator + reporte de revisión de código sin hallazgos de drift. | active — se resuelve junto con TD-MSF-API-006. |
| TD-MSF-STAB-001 | JWT default inseguro. `jwt.adapter.ts` usa `process.env.JWT_SECRET \|\| 'merkee-shop-dev-secret-change-in-production'`: secreto débil hardcodeado si la variable ausenta. `.env.example` no documenta `JWT_SECRET`. Riesgo de seguridad en producción. | Executor (incremento estabilización, STAB-B3) | `jwt.adapter.ts` lanza error de arranque en `NODE_ENV=production` si `JWT_SECRET` ausente o < 32 bytes; default solo en desarrollo con advertencia; `.env.example` documenta `JWT_SECRET`; test verifica fail-fast. | Test de fail-fast en producción + diff de `.env.example` con `JWT_SECRET` documentado. | active — bloqueante para producción; se cierra con STAB-B3. |
| TD-MSF-STAB-002 | ROP activate-admin drift. `activate-admin.use-case.ts` tiene `try/catch` técnico en `application`; el puerto `ActivateAdminUnitOfWorkPort.run` devuelve `Promise<T>` (no `Result`) y el adapter no traduce excepciones. Contradice Master Spec §ROP y el patrón de provision/bootstrap. | Executor (incremento estabilización, STAB-B1) | El puerto devuelve `Result<T, DomainError>`; el adapter captura y traduce a `fail(technicalFailure())`; `activate-admin.use-case.ts` sin `try/catch` técnico. | Test estático sin `try/catch` en application + test del adapter traduciendo fallo técnico + `npm run depcruise` sin violaciones. | active — bloqueante para revalidación; se cierra con STAB-B1. |
| TD-MSF-STAB-003 | `must_change_password` drift en replay de provisión. OpenAPI declara `const: true`; el código replay devuelve `user.mustChangePassword` (dinámico) y `schemas.ts` tipa `boolean`. Decisión STAB-DEC-01 Opción A: replay devuelve `true`. | Executor (incremento estabilización, STAB-B2) | `provision-admin-user.use-case.ts` fuerza `must_change_password: true` en replay; `schemas.ts` tipa `must_change_password: true` (literal); OpenAPI `const: true` sin cambio. | Test de replay tras activación devuelve `true` + diff de `schemas.ts` con literal `true` + OpenAPI sin cambio. | active — bloqueante para revalidación; se cierra con STAB-B2. |
| TD-MSF-STAB-004 | ROP JWT verify drift. `JwtAdapter.verify` propaga excepciones de `jwt.verify` (`TokenExpiredError`/`JsonWebTokenError`/`NotBeforeError`) sin traducir a `DomainError`; `JwtPort.verify` devuelve `Promise<JwtPayload>` (no `Result`); el controller `identity.controller.ts` inyecta `JwtAdapter` concreto (no `JwtPort`) y captura la excepción con `try/catch`. Contradice Master Spec §ROP y ADR-017. | Executor (incremento estabilización, STAB-B5) | `JwtPort.verify` devuelve `Result<JwtPayload, DomainError>`; `JwtAdapter.verify` captura excepciones de `jwt.verify` y traduce a `DomainError` del catálogo existente (`AUTHENTICATION_REQUIRED` 401 para token inválido/expirado; `TECHNICAL_DEPENDENCY_FAILURE` 500 para error técnico inesperado) sin causa/PII; el controller `extractSessionId`/`extractUserId` consume `Result` del puerto `JwtPort` (no del adapter concreto) sin `try/catch`; DI en `identity.module.ts` actualizada (controller inyecta `JwtPort`). | Test del adapter traduciendo `TokenExpiredError`/`JsonWebTokenError`/`NotBeforeError` a `DomainError` del catálogo existente sin causa/PII + test del controller consumiendo `Result` del puerto sin `try/catch` + `npm run depcruise` sin violaciones (controller→`JwtPort`, no →`JwtAdapter` concreto). | active — bloqueante para revalidación; se cierra con STAB-B5. No cubre `iss`/`aud`/`typ` ni el proveedor JWT (decisión pendiente) ni el fail-fast del secreto (B3). |

## Cambios completados — no son deuda activa

| ID | Evidencia | Estado |
|---|---|---|
| TD-MSF-ID-002-C01 | La secuencia aplicada `007_idempotency_records`, `008_idempotency_records_purge_index`, `009_idempotency_records_response_json_rename`, `010_idempotency_records_response_json_backfill`, `011_idempotency_records_response_json_normalize`, `012_idempotency_records_response_json_validate` y `013_idempotency_records_response_json_strict_validate` deja `response_json` con snapshot mínimo y validación estricta. | resolved-by-verified-implementation |
| TD-MSF-ID-002-C02 | El replay se reconstruye desde el recurso actual; no persiste respuesta completa ni PII. | resolved-by-verified-implementation |
| TD-MSF-ID-002-C03 | Scheduler local y bridge de métricas productivas están implementados; el adapter in-memory es solo de pruebas. Esto no resuelve la decisión operativa AWS de TD-MSF-ID-002-03. | resolved-by-verified-implementation |
| TD-MSF-ID-002-C04 | El bootstrap valida `role=admin` antes del no-op. | resolved-by-verified-implementation |
